# 更新日志

本项目按版本记录安装用户可见的变化。验证证据和维护细节见
[MAINTENANCE.md](MAINTENANCE.md)。

## 0.43.0 - 2026-08-27

### 修复抖音跨视频弹幕管理器沿用旧记录

- 修复抖音精选/推荐流切换视频后，弹幕屏蔽管理器仍显示上一个甚至更早视频弹幕发送者的问题。
- 弹幕会话现在使用当前路由和播放器可靠视频标识隔离；同一路由复用播放器时，播放器身份变化也会触发缓存清理。
- 切换视频时清空旧记录、搜索和选中状态，关闭属于旧视频的弹幕管理器，并把收集范围限制在当前播放器。
- 增加异步时间轴扫描的会话代次与取消保护，旧视频的扫描不会继续向新视频写入记录，也不会恢复旧视频的播放时间。
- 保持原有安全边界：未加载、无可靠发送者身份或未出现在页面中的弹幕不被猜测加入名单；所有屏蔽仍只写入本地名单。

### 验证边界

- **`real-site verified`**：2026-08-27，在用户已登录的专用持久 Chrome 中直接注入版本元数据提升前的工作区候选，脱敏页面形式为
  `douyin.com/jingxuan?...`。该候选与 `0.43.0` 的行为代码相同，仅版本/构建标识不同；实际点击下一个视频后，初始管理器
  12 行、下一视频管理器 12 行，旧视频 12 个身份全部不再出现，旧管理器自动关闭，新视频记录存在；换片完成后继续采样
  10 秒/20 次，CDP 心跳错误 0、最大单次响应 25ms。
- **`real-site verified`**：同一专用 Chrome 的同视频 30 秒基线采样 55 次，版本元数据提升前候选运行期间心跳错误 0，最大单次
  响应 224ms。
- **`structure regression`**：抖音播放器复用、URL 不变而 `video_<id>` 改变、旧弹幕层移除并挂载新弹幕层的夹具通过；
  `node test/adapters.cjs` 为 21/21，完整受影响本地矩阵通过。
- **`blocked`**：未登录公开探针停在抖音验证码中间页；版本提升为 `0.43.0` 后在专用 Chrome 的两次精确短探针中均未
  渲染出带发送者身份的弹幕，无法重新执行双视频断言；同日重复 90 秒真实采样还受平台动态回收影响出现短暂空节点。
  这些结果未被宣称为 `0.43.0` 的长时无缺口通过，也没有记录 CDP 超时或脚本异常。

### 版本/发布状态

- `0.43.0` 为当前发布版本；本版本只包含抖音弹幕跨视频会话隔离修复及对应测试、探针和文档。
- 固定 tag 为 `v0.43.0`；GitHub Release 与最终提交信息将在发布动作完成后回填到本条。

## 0.42.0 - 2026-08-27

### 三平台统一评论管理与“屏蔽该楼回复”

- B站、抖音、微博的评论批量入口统一为全窗管理器：按作者去重，显示示例评论、评论数量和主评论/回复层级，
  支持搜索、筛选后全选、多选一次提交以及撤销；已在本地名单中的作者不会重复列出。
- B站管理器通过只读的根评论/子回复分页读取尽量补齐未手动展开的回复；达到安全上限、接口不完整或时间未知时，
  管理器明确显示“部分”，不把部分结果冒充全量。
- 抖音评论管理器保留真实 DOM 展开和安全滚动的懒加载策略，增加统一搜索、多选和尽量加载；弹幕管理器继续支持
  发送者搜索、批量屏蔽、时间轴扫描以及播放位置/状态恢复。
- 微博评论管理器使用当前路由内已观察的 DOM、展开结果和安全滚动缓存；继续兼容详情页、用户主页页内评论、楼中楼
  portal 和虚拟回收行，不把帖子作者或预览评论混入错误的评论范围。
- B站、抖音、微博的主评论入口旁新增「🧵 屏蔽该楼回复」：只写入本地身份名单，包含主评论作者和已加载/已读取的回复
  作者；无法完整读取时会提示“部分”，子评论自身不显示这个楼操作入口。
- 增加同一文档运行时防重复锁。调试重注入或脚本时序重复时，第二次执行只记录重复计数，不再叠加扫描器、观察器、
  定时器和设置入口，降低微博虚拟列表重复协调造成页面卡顿或无响应的风险。
- 所有平台操作仍只调用脚本自身的本地存储和 UI，不触碰举报、官方拉黑、关注、发帖等平台写入控件。

### 验证边界

- **`real-site verified`**：2026-08-27，专用登录态 Chrome 中的 B站视频、抖音视频弹窗和微博热门帖子流均直接注入
  当前 `0.42.0` 源码并完成相应只读交互；三者的源码版本/构建标识与工作区一致。B站评论/弹幕管理器与楼操作通过，
  抖音评论/弹幕管理器与单条入口通过，微博 90 秒帖子流滚动无视觉跳变、无心跳错误。
- **`structure regression`**：统一管理器 3/3、B站 32/32、跨平台适配器 21/21、通用 UI 14/14、抖音推荐流
  2/2、微博虚拟列表回放 11/11；覆盖作者去重、搜索、多选、一次提交、撤销、未手动展开回复的读取契约、评论/帖子
  补位、回收行复用和重复注入防护。
- **`blocked`**：公开隔离会话的抖音入口可能停在验证码中间页，微博公开详情页没有可测的活动顶层虚拟评论 spacer；这些
  隔离子条件不替代已完成的专用登录态验证。抖音和微博的“全部子评论”仍受当前页面 DOM、接口可用性和安全上限限制，
  结果不足时必须显示部分。

### 版本/发布状态

- `0.42.0` 为当前发布版本；功能实现提交为 `05216b3`，本版本包含三平台评论管理器、主评论楼操作和同文档运行时防重复保护。
- 固定 tag 为 `v0.42.0`，GitHub Release：<https://github.com/a2787/ub-utils/releases/tag/v0.42.0>；详细验证证据和限制见
  `MAINTENANCE.md`。

## 0.41.0 - 2026-08-26

### 修复微博无限流推荐页滚动闪跳与整帖屏蔽空位

- 修复微博热门/推荐等无限流页面中，作者快捷入口插入帖子 `header` 后改变回收物理行布局，
  在继续加载或滚动时出现一两帧上跳再回位的问题。微博虚拟帖子卡片现在使用挂在 `body` 的独立
  页面门户，按作者链接的视口坐标定位，不改写 `article/header` 的高度、回收行 `transform` 或列表 spacer。
- 修复在无限流中通过作者入口本地拉黑整条帖子后，帖子消失但原位置留下空白的问题。脚本会记录
  被隐藏帖子虚拟行的原始高度，同时把累计位移写入直接内容层并缩短列表 spacer，后续帖子会从原位置
  补上；撤销屏蔽后会恢复原始高度、内容层 transform 和列表基线。
- 物理行复用时门户会重新读取当前卡片作者身份；卡片内预览评论仍按嵌套普通评论处理，不会误接管
  详情页顶层评论的虚拟补位。
- 扫描器忽略脚本自身 UI 的纯 `childList` 变化；只有已经存在本地虚拟补位工作的列表才会因平台结构变化
  触发协调，避免作者入口反过来启动整表布局扫描。
- 记录浏览器 CSSOM 规范化后的实际像素值，避免高精度 `px` 被浏览器截断后被误判为平台新基线，
  反复减去隐藏高度造成页面越来越空或无响应；新增整帖屏蔽期间的 spacer/transform 回写回归。
- 探针在本轮导航完成后移除 `addScriptToEvaluateOnNewDocument` 注册，复用专用标签页不会叠加多份候选
  初始化脚本；保留当前源码 SHA-256、版本和构建标识校验。
- 新增 80 行/100 次作者节点复用的无限流压力回归，并保留详情页、用户主页、回收行重绘和长期平台回写回归。

### 验证边界

- **`real-site verified`**：2026-08-26 在用户已登录的专用持久 Chrome 中，使用当前源码直接注入脱敏形式
  `weibo.com/...` 的热门/推荐无限流页面。初始真实页渲染 7 张帖子/7 个回收行；心跳 10 次、p95 7ms、
  错误 0，5.1 秒滚动监视无视觉跳变。随后通过脚本自己的作者入口确认一次本地屏蔽：目标帖子高度压为 0，
  下一条内容立即补到目标原位置；继续动态加载并滚动 90 秒，共 1,422 次 100ms 采样、90 次滚动，
  最多 17 张帖子，视口内卡片自然间距峰值 8px，未屏蔽活动空行峰值 0，页面始终可响应。源码 SHA-256
  为 `d507f008b5a401ed68d8a79e3356e79f20a7eee5b7f432bb3746e3ccf5043cab`。测试身份只写入本轮内存 stub，
  已在结束时撤销；未读取 Cookie，也未点击微博举报、官方拉黑、关注或发帖控件。
- **`structure regression`**：`node test/weibo-replay.cjs` 11/11；`node test/weibo-replay.cjs --git-ref=v0.40.0`
  为 8 项通过、3 项失败，旧版失败项包含行内作者入口、整帖屏蔽不补位和 80 行复用触发虚拟同步；
  通用 UI 13/13、状态 7/7、B站 32/32、抖音 2/2、跨平台适配器 21/21 均通过，三份 JS 语法检查和
  `git diff --check` 通过。
- **`blocked`**：`node test/real-platform-probe.cjs weibo --verify-local` 自动发现的公开详情页虽然加载评论，
  但没有带可测 spacer 的活动顶层虚拟评论；`node test/maintenance-check.cjs` 还受到抖音验证码中间页影响。
  这些隔离子条件不能替代已完成的专用登录态热门流验证，也没有被改写成通过。

### 版本/发布状态

- `0.41.0` 已完成本轮真实热门流与本地回放验证，实现提交为
  `fa978f0f39e03374029912b9f1c2a3dc037b236c`；发布边界使用 `v0.41.0` tag 和同版本 GitHub Release，
  不为本轮修复额外消耗版本号。

## 0.40.0 - 2026-08-26

### 修复微博帖子详情页回收行重用后的评论空洞

- 修复 v0.39.0 在微博帖子详情页展开全部评论后，屏蔽主评论的位置仍可能留白的问题。
  微博回收物理行重新激活时会清掉直接内容层的本地补位 transform；上一版等待平台静默窗口，
  或只观察外层行，导致列表 spacer 已补偿但浏览器绘制仍回到未补位状态。
- 详情页虚拟列表现在只观察已确认的直接虚拟行、直接内容层和列表成员变化；平台重写在同一
  MutationObserver 批次内立即同步，脚本自己的 style 写回会被识别并忽略，不再触发第二轮整列表扫描。
- 保留用户主页页内评论的嵌套结构保护：这类评论仍只折叠自身，不接管外层帖子回收行。

### 验证边界

- **`real-site verified`**：2026-08-26 在用户确认登录的专用持久 Chrome 中直接注入当前 `0.40.0` 候选，
  使用脱敏形式 `weibo.com/...` 的帖子详情页只读滚动 90 秒；源码 SHA-256 为
  `4cb1a305e855c03f18d582f038d887ee6cbf1931bd21cede39f5894319323265`。完成 1802 次采样，目标评论保持隐藏，
  活动空行峰值 0、可见间隙峰值 0，CDP 心跳 p95 为 4ms、心跳错误 0；诊断记录立即重同步 34 次。仅使用内存
  名单 stub，不读取 Cookie，也不点击微博举报、官方拉黑、关注或发帖控件。
- **`structure regression`**：`node test/weibo-replay.cjs` 8/8；旧版 `v0.38.0` 在新增详情页的两项断言上失败，
  当前候选与用户主页嵌套保护共同通过；通用 UI 13/13、状态 7/7、B 站 32/32、抖音 2/2、跨平台适配器 21/21。
- **`blocked`**：隔离公开微博探针若没有可测的活动顶层虚拟评论 spacer，只记录该子条件受阻，
  不替代专用登录态详情页验证。

### 版本/发布状态

- `0.40.0` 已通过用户在专用 Chrome 保留页面中的手工验收；提交
  `46849119394b2595f7ec2281e490769c797b2422` 已推送到 `origin/master`，`v0.40.0` tag 已创建并推送，
  GitHub Release：<https://github.com/a2787/ub-utils/releases/tag/v0.40.0>。

## 0.39.0 - 2026-08-26

### 组合微博详情页与用户主页的评论补位路径

- 修复微博帖子详情页展开评论后，顶层评论实际位于虚拟行内容层的
  `wbpro-list > item1` 时，脚本把它误判为普通嵌套评论，导致隐藏评论所在位置留下空洞、
  后续评论不能自动补位的问题。
- 详情页只在已捕获的 `woo-panel-main` 结构中接入这条虚拟补位路径；用户主页帖子内部更深处的
  `wbpro-list` 仍只折叠评论自身，不会移动整条帖子或下方帖子。
- 新增详情页 `wbpro-list > item1` 顶层虚拟评论回放，和原有用户主页嵌套评论、平台持续回写、
  物理行复用及撤销回归合并验证。

### 验证边界

- **`real-site verified`**：2026-08-26 在用户确认登录的专用持久 Chrome 中直接注入当前 `0.39.0` 候选，
  详情页展开评论后屏蔽一条顶层评论；目标评论保持隐藏，后续内容层连续补位。90 秒内完成 1805 次采样，
  可见间隙峰值为 0、未屏蔽活动空行峰值为 0、CDP 心跳 p95 为 5ms，无登录/验证码拦截或心跳超时。
  探针使用内存名单 stub，未读取 Cookie，也未点击微博举报、官方拉黑、关注或发帖控件。
- **`structure regression`**：`node test/weibo-replay.cjs` 7/7、`node test/adapters.cjs` 21/21；新增详情页
  `wbpro-list > item1` 断言通过，用户主页嵌套评论保护仍通过。旧 `v0.38.0` 在新增详情页断言上失败，
  证明回归能够捕获本次缺陷。
- **`blocked`**：本轮 v0.39.0 用户主页登录态探针没有可解析的评论目标；隔离公开微博探针没有发现可测的
  活动顶层虚拟评论 spacer。两项均未改写为线上通过。

### 版本/发布状态

- v0.39.0 的源码候选已在专用 Chrome 中保留供用户验收；Git 提交、tag、推送和 GitHub Release 待用户确认专用页面行为后收口。

## 0.38.0 - 2026-08-26

### 修复微博用户主页页内评论展开后的闪动与下方帖子受影响

- 修复用户主页中点击帖子自带的页内评论展开入口后，脚本把嵌套在帖子内部
  `wbpro-list` 的评论误识别为整条帖子的 `vue-recycle-scroller` 虚拟行，导致屏蔽评论时
  整帖和下方帖子一起移位、闪回甚至拖慢页面的问题。
- 微博虚拟补位现在只对直接挂在虚拟行或其直接内容层下的评论行生效；嵌套在帖子内容层更深处的
  评论仍会独立无占位隐藏，不会接管外层帖子的回收位置。
- 增加用户主页页内评论展开的人工合成回放，覆盖屏蔽、下方帖子连续性、物理行占位/复用和撤销；
  运行时诊断增加“忽略嵌套虚拟行”计数，默认关闭，不改变用户界面和名单格式。

### 验证边界

- **`real-site verified`**：2026-08-26 在用户确认登录的专用持久 Chrome 中直接注入当前 `0.38.0` 候选，
  打开 `weibo.com/...` 用户主页并点击帖子页内评论展开入口；评论列表展开后，内存名单命中的目标评论保持隐藏，
  90 秒内无登录/验证码拦截、CDP 心跳错误、可见空白回收行或可见间隙，诊断记录忽略嵌套虚拟行 1396 次。
  同日详情页 90 秒只读验证也无心跳错误、空白回收行或可见间隙。探针未读取 Cookie，也未点击微博举报、
  官方拉黑、关注或发帖控件。
- **`structure regression`**：`node test/weibo-replay.cjs` 6/6、`node test/adapters.cjs` 21/21；回放覆盖普通
  虚拟评论补位、平台持续回写、脚本写回隔离、弹窗回复撤销、长期回写和用户主页嵌套评论；当前源码语法、通用 UI、
  状态、B站、抖音和跨平台矩阵均通过。
- **`blocked`**：本轮隔离公开微博探针成功加载并验证了评论/楼中楼入口，但发现的详情页没有暴露可测的活动“直接
  顶层虚拟评论”行，因此该页面的 spacer 子检查按规则记为 `blocked`；这不替代已完成的专用登录态验证，也未被写成
  线上通过。

### 版本/发布状态

- `v0.38.0` 已发布；提交 `0c94669` 已推送到 `origin/master`，`v0.38.0` tag 已创建并推送，GitHub Release：
  <https://github.com/a2787/ub-utils/releases/tag/v0.38.0>。既有 `v0.37.0` 及更早版本保留，未覆盖。

## 0.37.0 - 2026-08-26

### 修复微博虚拟评论屏蔽后的空白、频闪与页面无响应

- 微博虚拟列表的正常补位改为移动被屏蔽虚拟行的直接内容层，保留外层 `transform` 供回收器管理，避免脚本与平台反复争夺同一个外层位置。
- 清理物理行复用后的过期屏蔽状态；撤销屏蔽时等待待处理同步完成，恢复评论内容层、包装层和列表状态，避免下方评论整体消失。
- 平台单独回写 spacer 时只记录新的平台基线，不再触发整列表扫描和脚本自激写回；虚拟行/结构变化使用静默窗口协调，降低微博页面长期运行时的布局压力。
- 增加 16 行真实列表契约、96 行持续回写、评论内部样式变化、弹窗回复撤销和物理行复用回归；专用登录态探针记录源码 SHA-256、运行构建、心跳和隐藏行采样。

### 验证边界

- **`real-site verified`**：2026-08-26 专用持久 Chrome 登录态中直接注入当前 `0.37.0` 候选，在两个实际有评论的 `weibo.com/...` 详情页各运行 90 秒；两组均无 CDP 心跳错误，未屏蔽活动虚拟行零高度采样为 0，屏蔽目标保持隐藏，未触碰微博举报、官方拉黑、关注或发帖控件。
- **`structure regression`**：微博回放 6/6、跨平台适配器 21/21，覆盖直接内容层补位、平台 spacer 持续回写、弹窗回复撤销、长期回写和物理行复用。
- **`blocked`**：个人主页目标本轮没有可解析评论候选；隔离公开探针还被环境 `ERR_NETWORK_ACCESS_DENIED` 阻断。这两项均未被写成线上通过。

### 版本/发布状态

- `v0.37.0` 已发布；提交 `915c333` 已推送到 `origin/master`，`v0.37.0` tag 已创建并推送，GitHub Release 已发布：
  <https://github.com/a2787/ub-utils/releases/tag/v0.37.0>。构建标识为 `0.37.0-weibo-virtual-content-offset-douyin-manager`；
  既有 `v0.35.0`、`v0.36.0` 保留，未覆盖。

## 0.36.0 - 2026-08-25

### 修订已安装用户可识别的版本号

- 将已经过微博真实站点和本地回归验证的 `0.35.0` 实现重新编号为 `0.36.0`；本版本不改变平台
  适配、屏蔽逻辑或用户界面，只修订 userscript `@version` 与运行构建标识。
- 登录态探针把微博初始隐藏确认从固定 500ms 改为最多 3 秒轮询，避免虚拟列表首轮扫描较慢时误报；
  这只影响验证工具，不改变 userscript 行为。
- 这样此前已安装 `0.35.0` 的 Tampermonkey 用户可以识别到更新；已发布的 `v0.35.0` tag 保留，
  不覆盖历史版本。

### 验证边界

- **`real-site verified`**：行为代码沿用 2026-08-25 已验证的微博 v0.35.0 实现；本次只改版本元数据，
  不改变已验证的评论补位、spacer 稳定性或本地屏蔽路径。
- **`structure regression`**：版本号修订后重新运行 userscript 语法、通用 UI、状态、B 站、跨平台适配器、
  抖音和微博回放检查；结果记录在本轮交接中。
- **`blocked`**：隔离网络探针仍可能因环境返回 `ERR_NETWORK_ACCESS_DENIED`；这不影响专用 Chrome 中已完成的
  登录态只读验证，也没有读取 Cookie 或点击平台写入控件。

### 版本/发布状态

- `v0.36.0` 已替代已安装的 `0.35.0`：提交 `fcb29a8` 已推送到 `origin/master`，
  `v0.36.0` tag 已创建并推送，GitHub Release 已发布：
  <https://github.com/a2787/ub-utils/releases/tag/v0.36.0>。已发布的 `v0.35.0` 不覆盖。

## 0.35.0 - 2026-08-25

### 修复微博虚拟评论屏蔽后的尾部空白

- 修复微博顶层评论被 `vue-recycle-scroller` 回收时，评论行虽然已经上移，所属
  `item-wrapper` 仍保留旧 `min-height`，导致被屏蔽位置下方继续出现整段空白的问题。
- 屏蔽期间按同一虚拟列表中实际隐藏行的高度临时扣减 `min-height`（或显式 `height`），并
  监听微博后续重排对 spacer 的回写；撤销名单时恢复平台原始尺寸。
- 保留已有的异常 `transform` 连续性修复、非活动回收行保护和楼中楼独立隐藏行为。
- 修复微博普通评论初始扫描和平台 `style` 回写时的性能问题：没有本地屏蔽行的虚拟列表不再
  触发补位扫描；有屏蔽行时只观察虚拟行和列表 spacer，缓存行高、忽略脚本自身写回；平台改写
  spacer 时即时纠正，完整行协调按列表节流，避免评论区进入自激重排。

### 验证边界

- **`real-site verified`**：2026-08-25 隔离未登录公开微博详情页注入当前工作区源码，顶层虚拟评论
  隐藏 63px 后 spacer 从 1329px 稳定缩至 1266px，700ms 采样无回弹，撤销恢复；未触碰微博平台
  举报、官方拉黑、关注或发帖控件。
- **`real-site verified`**：2026-08-25 用户确认已登录的专用持久 Chrome 中，直接注入当前工作区源码运行
  三组各 90 秒的微博只读探针（基线、空名单、内存名单屏蔽）。三组均无登录/验证码拦截、CDP 心跳错误或
  页面无响应；候选运行标识和源码 hash 一致，空名单可解析评论，内存名单屏蔽目标保持隐藏。探针未读取
  Cookie，也未点击微博举报、官方拉黑、关注或发帖控件。
- **`structure regression`**：当前工作区源码的微博回放 5/5、跨平台适配器 21/21；96 行、1 个屏蔽行的
  持续回写和评论内部 style 回写均无静止后的自激布局读取。旧 v0.34.0 在同一回放中 4/5 项失败，
  持续阶段布局读取 36,765 次、静止窗口读取 9,120 次，并继续漏掉 spacer 补偿。
- **`blocked`**：同日最初两次探针尝试曾错误报告登录页/候选未注入；复核后确认原因是探针的模板正则、
  返回值换行和内存 GM shim 语法缺陷，已在本轮修正并由上述 90 秒探针重新验证。该历史阻断不代表微博
  页面本身被登录或验证码拦截。

### 版本/发布状态

- userscript `@version` 提高为 `0.35.0`，构建标识为
  `0.35.0-weibo-virtual-spacer-douyin-manager`。
- `v0.35.0` 已完成当轮验证并发布；由于用户已安装过该版本，本轮以 `v0.36.0` 作为可识别更新重新发布
  同一已验证实现。

## 0.34.0 - 2026-08-24

### 优化抖音评论与弹幕管理器

- 抖音评论管理器和弹幕管理器统一移到页面右下角，与齿轮组成同一列；从下到上依次是设置、弹幕、评论，
  不再遮挡播放器左下区域。
- 抖音评论管理器新增「搜索已加载评论」和「尽量加载评论」；搜索会保留虚拟列表回收前已经观察到的
  可靠作者，尽量加载只读滚动安全识别的评论容器并等待懒加载，再回到管理器更新列表。
- 抖音弹幕管理器新增搜索和「尽量加载弹幕」；按钮会在用户主动点击后按播放器时长扫描若干时间点，
  尝试触发网页弹幕渲染，结束时恢复原播放位置和播放状态。
- 两种尽量加载都不调用抖音私有接口、不触发举报/官方拉黑等平台写入；虚拟列表回收、未渲染节点和
  平台未公开的数据仍不会被猜测成全量。

### 验证边界

- **`structure regression`**：抖音人工合成夹具覆盖右下三按钮布局、评论搜索与虚拟滚动懒加载、弹幕
  搜索与时间轴扫描/播放状态恢复，以及原有单条/批量屏蔽路径。
- **`blocked`**：2026-08-24 默认隔离抖音探针实际停在验证码中间页、候选数为 0；本轮未在用户登录态
  调试浏览器中运行探针，因此新增的搜索和尽量加载能力没有被写成真站已验收。

### 版本/发布状态

- userscript `@version` 提高为 `0.34.0`，构建标识为 `0.34.0-douyin-manager-layout-search-loader`。
- 本轮源码可作为候选更新；抖音新增路径缺少本轮 `real-site verified` 证据，暂不创建 v0.34.0 tag/Release。

## 0.33.0 - 2026-08-24

### 抖音视频页新增弹幕多选屏蔽管理器

- 抖音视频页右下角新增「🚫 抖音弹幕屏蔽(N)」入口；它只收集当前视频页面实际观察到、且带可靠
  `data-danmaku-user-id` 的弹幕发送者。
- 同一发送者的多条弹幕在管理器中合并为一行，支持搜索、全选、逐项勾选和批量本地屏蔽；切换视频
  会清空上一视频的观察列表，不读取抖音私有接口，也不触发平台举报、拉黑或其他写入。
- 抖音左下角原有的「🚫 抖音评论屏蔽(N)」管理器继续支持明确回复入口的自动展开、全选和批量屏蔽；
  本轮只补齐视频弹幕管理器，不改微博适配器或微博补位逻辑。

### 验证边界

- **`real-site verified`**：2026-08-24 隔离未登录、自动发现的脱敏
  `bilibili.com/video/...` 页面注入当前 v0.33.0 源码后，实际看到左下评论批量入口和右下弹幕
  多选管理器；管理器列出 27 个文案组、26 位发送者，并完成单组/批量屏蔽与撤销，另完成 10 条
  悬浮弹幕的本地入口交互。没有触碰 B 站平台写入控件。
- **`structure regression`**：`node test/adapters.cjs` 21/21、`node test/run.cjs` 13/13、
  `node test/quickblock.cjs` 32/32、`node test/douyin.cjs` 2/2；覆盖抖音评论 portal 单条入口、
  评论管理器展开/批量、视频弹幕发送者去重/多选/批量及名单撤销。
- **`blocked`**：2026-08-24 抖音隔离探针停在「验证码中间页」，登录态只读探针连接
  `127.0.0.1:9222` 被拒绝；因此 v0.33.0 的两个抖音新管理器尚未获得本轮真站结果，不能把夹具
  通过写成线上验收。

### 版本/发布状态

- userscript `@version` 提高为 `0.33.0`，构建标识为 `0.33.0-douyin-danmaku-manager`。
- 本轮源码可作为候选更新；由于抖音新入口的真站探针仍为 `blocked`，暂不创建 v0.33.0 tag/Release。

## 0.32.0 - 2026-08-24

### 修复微博近阈值虚拟定位导致的评论区空白

- 微博虚拟列表现在会把相邻活动行的连续性作为异常判断依据，不再把约 `-20000px` 的错误
  `translateY` 当成合法平台基线；异常行会按前一条活动行的真实位置和高度重建，再扣除本地
  屏蔽行的高度。
- 保留对科学计数法异常位置和平台周期性回写的处理，并继续让 `opacity:0` 的回收行保留平台
  控制权，避免页面卡顿或滚动后整段空白。
- 微博回放夹具改为覆盖当前真站的顶层行契约、`data-index` 跳号和约 `-20000px` 回写；维护者
  可用旧提交复核该缺陷确实会失败。

### 验证边界

- **`real-site verified`**：2026-08-24 当前用户 Chrome 的精确微博页实际运行
  `0.31.0-weibo-virtual-row`；只读观察到被屏蔽行后续活动行稳定处于约 `-20000px`，评论区
  仍为空白。这是本轮修复前的真实失败复现。
- **`structure regression`**：当前 v0.32.0 源码的微博回放、连续平台回写和撤销断言 3/3 通过；
  已推送的 v0.31.0 源码在同一近阈值夹具上顶层虚拟评论断言失败。
- **`blocked`**：当前用户 Chrome 尚未用页面构建标识确认 v0.32.0 已安装，因而没有把精确页面的
  修复后结果写成已验收，也暂不创建 v0.32.0 tag/Release。

## 0.31.0 - 2026-08-24

### 修复微博顶层虚拟评论导致的整段空白

- 识别微博详情页被 `vue-recycle-scroller__item-view > .wbpro-scroller-item` 包裹的顶层评论，
  不再因虚拟化后脱离 `.wbpro-list > .item1` 而丢失行高和屏蔽状态。
- 解析微博平台写回的科学计数法 `transform`，并在出现约百万像素的异常活动行位置时，按前一条
  活动行的真实高度重建连续位置；平台周期性回写异常值时仍会保持补位，撤销后恢复原始相邻位置。
- 维护者可运行 `node test/maintenance-check.cjs`，由当前源码自行完成完整结构矩阵、微博回放/压力
  回归和隔离真站探针，不依赖用户先把候选脚本装进自己的 Tampermonkey。

### 验证边界

- **`structure regression`**：顶层虚拟评论回放、平台反复回写和撤销 3/3 通过；v0.28.0 在同一回放
  上 3 项均失败，并复现活动行约负一百万像素位移。
- **`real-site verified`**：2026-08-24 隔离未登录公开微博详情页注入 v0.31.0 当前源码，观察到
  21 条评论、20 个身份和 23 个行内入口；楼中楼本地屏蔽后根评论从 139 上移到 87，撤销恢复，
  探针无页面错误。
- **`blocked`**：当前用户 Chrome 的精确微博页仍运行 v0.29.0，尚未用页面构建标识确认 v0.31.0
  已安装；因此不把登录态精确页面写成已复核，也暂不创建 v0.31.0 tag/Release。

## 0.30.0 - 2026-08-24

### 增加“当前运行代码”维护验收标识

- 设置面板和右下角齿轮显示实际运行版本与源码构建标识，并提供 `data-ob-*` 机器可读属性。
- `window.OB.runtime` 暴露同一组只读标识，供维护探针在当前浏览器页面核对安装内容。
- 通用回归会同时比对 userscript 头版本、源码构建标识、页面齿轮属性和设置面板显示，避免只凭
  Tampermonkey “已是最新”就开始验收平台修复。
- 新增 `node test/maintenance-check.cjs` 维护自检，以及基于真实微博虚拟列表契约的回放压力测试；
  回放会模拟平台反复回写位置、回收行和撤销，旧 v0.28.0 源码在同一回放上会失败。

### 验证边界

- **`structure regression`**：运行时标识契约由通用浏览器回归覆盖；候选版本发布前会复跑完整矩阵。
- **`structure regression`**：当前源码的微博回放/压力测试两项通过；v0.28.0 源码在相同临时超大行高
  夹具上产生约负一百万像素的后续行位移并失败。
- **`real-site verified`**：2026-08-24 当前用户 Chrome 的微博页面实际运行 v0.29.0，脚本自身检查更新
  显示“已是最新 (v0.29.0)”，但旧页面没有 v0.30.0 的构建属性；这确认了本轮维护门禁此前确实缺失。
- **`blocked`**：v0.30.0 候选尚未安装到当前用户 Chrome，尚未用新标识确认当前页面已切换到候选源码；
  在该确认完成前，不把微博修复结果写成已验收，也不创建 v0.30.0 正式 Release。

### 限制

用户脚本无法在运行时自替换。检查更新后仍需在 Tampermonkey 安装页确认一次，再刷新原平台页面；
若版本或构建标识不匹配，维护流程必须停在 `blocked`。

## 0.29.0 - 2026-08-24

### 修复微博登录态首轮高度异常导致的整段空白

- **读取真实内容高度**：微博虚拟行优先从 `.wbpro-scroller-item` 直接内容层读取评论实际高度，
  不再把登录态首轮渲染的临时超大 `item-view` 高度缓存为屏蔽行高度。
- **拒绝异常补位来源**：超过合理上限的缓存高度和虚拟行 transform 差值不再参与后续行补位，
  防止再次写出负一百万像素的 `!important` 位移。

### 验证边界

- **`real-site verified`**：2026-08-24 隔离未注入脚本的同一微博详情页捕获到原始行位置
  `0/102/165/356/419`，被屏蔽行原始高度约 `191px`；隔离内存调试版使用 v0.29 代码后
  记录 `height=191`，后续行正确移动到 `165/228`。
- **`real-site verified`**：2026-08-24 当前用户 Chrome 的 v0.28.0 页面刷新后仍实际出现
  第 3 行约 `-1.0001e+06px !important`，确认 v0.28 未解决登录态路径；没有点击微博举报、
  官方拉黑、关注或发帖。
- **`structure regression`**：新增“虚拟行临时高度为 1,000,100px、直接内容层为正常高度”
  夹具；v0.28.0 在同一夹具上 20/21 失败，v0.29.0 21/21 通过。
- **`blocked`**：v0.29.0 尚未安装到当前用户 Chrome，精确页面的修复后结果需更新并刷新后复核。

### 限制

微博补位仍只处理可识别的绝对定位 `vue-recycle-scroller__item-view`，并只对虚拟行自身可确认
只包含被隐藏评论的情况做偏移补偿。

## 0.28.0 - 2026-08-24

### 修复微博非活动虚拟行被锁死导致的整段空白

- **保留回收器控制权**：微博虚拟列表中暂时回收的 `opacity:0` 行继续保留平台原始
  `transform`，不再写入本地补位的 `!important` 变换；行重新活动后才进行补位。
- **清理旧补位**：已被旧逻辑写过补位的非活动行会恢复原内联样式，避免更新脚本后仍
  继续把后续评论留在页面外。

### 验证边界

- **`real-site verified`**：2026-08-24 当前用户 Chrome 的微博详情页只读捕获到非活动行
  原始 `translateY(-9999px); opacity:0` 被旧版写成约负一百万像素的 `!important` transform，
  并确认后续评论因此全部不可见；未点击微博举报、官方拉黑、关注或发帖。
- **`structure regression`**：新增非活动回收行夹具；当前版本 21/21 适配器回归通过，旧版
  在同一夹具上失败，新版保持回收 transform，同时活动行补位与撤销恢复仍通过。
- **`blocked`**：v0.28.0 尚未安装到当前用户 Chrome，精确页面的修复后结果需用户更新脚本并刷新
  后在同一浏览器会话复核。

### 限制

微博补位仍只处理可识别的绝对定位 `vue-recycle-scroller__item-view`，并只对虚拟行自身可确认
只包含被隐藏评论的情况做偏移补偿。

## 0.27.0 - 2026-08-24

### 修复微博拉黑后的卡顿与整段空白

- **停止 style 反馈循环**：微博虚拟行的 `style` 变化只触发必要的补位，不再触发整页扫描；脚本重复写入相同 `transform` 时也不会制造新的属性变化。
- **保留回写补位**：微博页面把后续虚拟行恢复为原始位置时，脚本仍会立即重新计算补位，撤销本地屏蔽时恢复原始位置。

### 验证边界

- `real-site verified`：2026-08-24 隔离未登录微博公开详情页探针无页面错误，完成楼中楼展开、屏蔽、补位和撤销；观察到根评论仍可见，位置从 279 上移到 227 后恢复。
- `structure regression`：本轮适配器回归 21/21；新增虚拟行回写后的 style 变化次数上限断言，防止脚本自激扫描。
- `blocked`：当前用户 Chrome 仍需更新到 v0.27.0 后复核；本轮不能把旧版页面的卡顿结果写成新版本已验证。

### 限制

微博补位仍只处理可识别的绝对定位 `vue-recycle-scroller__item-view`，并只对虚拟行自身可确认只包含被隐藏评论的情况做偏移补偿。

## 0.26.0 - 2026-08-24

### 抖音菜单重复注入与微博虚拟行回弹修复

- **抖音评论菜单去重**：`pointerover/focusin` 事件路径只允许真实的
  `[data-e2e="video-comment-more-report"]` 叶子项注入；同一 tooltip 已存在的多余入口也会被清理。
- **微博补位抗回写**：监听虚拟行 `style` 重排，把微博后续行被页面恢复的原始 `transform` 重新作为基线补偿，避免评论短暂上移后再次留下空白。

### 验证边界

- `real-site verified`：2026-08-24 当前用户 Chrome 只读复现了 v0.25.0 的抖音 tooltip 事件路径，实际看到同一菜单出现 3 个脚本入口；微博当前页实际看到隐藏行高度为 0、后续行带脚本补位的 `!important` transform。另在隔离未登录公开微博详情页观察到 9 条评论、9 条可识别身份、0 个重复入口，楼中楼屏蔽后根评论仍可见且位置从 110 上移到 63，撤销恢复。未点击平台举报、官方拉黑或其他写入控件。
- `structure regression`：`node test/adapters.cjs` 21/21、`node test/run.cjs` 12/12、`node test/state.cjs` 7/7、`node test/quickblock.cjs` 32/32、`node test/douyin.cjs` 2/2；`node --check omniblock.user.js` 与 `git diff --check` 通过。新增抖音父层 pointerover 去重和微博虚拟行 style 回写断言。
- `blocked`：v0.26.0 尚未在当前用户 Chrome 页面更新后复核；默认隔离抖音探针仍可能停在验证码中间页，用户报告的具体微博页面仍需安装后确认。

### 限制

微博补位仍只处理可识别的绝对定位 `vue-recycle-scroller__item-view`，并只对虚拟行自身可确认只包含被隐藏评论的情况做偏移补偿。

## 0.25.0 - 2026-08-24

### 抖音重复入口与微博虚拟列表空洞修复

- **抖音评论菜单去重**：同一 `role="tooltip"` portal 内只对真实的
  `[data-e2e="video-comment-more-report"]` 叶子项注入「🚫 本地拉黑」，不再把 tooltip 容器、内容层和举报项重复当成三个菜单项。
- **微博后续评论补位**：对真实 `vue-recycle-scroller__item-view` 绝对定位虚拟行记录被隐藏行的原始高度，按该高度减少后续行的 `translateY` 偏移；撤销或名单删除时恢复原内联变换。
- **安全边界**：只有虚拟行自身可确认只包含被隐藏评论时才补偿后续行，根评论仍可见而仅子评论隐藏时不挪动整行。

### 验证边界

- `real-site verified`：2026-08-24 当前用户 Chrome 只读复现了微博页面的真实空洞结构：隐藏虚拟行高度为 0，后续行仍保留 `translateY(191px)`；同日抖音页面实际捕获 23 条评论和 portal 举报项。另在 v0.25.0 隔离公开微博详情页观察到楼中楼屏蔽后根评论从 139px 上移到 87px，撤销恢复。
- `structure regression`：`node test/adapters.cjs` 21/21、`node test/run.cjs` 12/12、`node test/state.cjs` 7/7、`node test/quickblock.cjs` 32/32、`node test/douyin.cjs` 2/2；`node --check omniblock.user.js` 与 `git diff --check` 通过。新增抖音 tooltip 单入口断言和微博绝对定位虚拟行偏移/恢复断言。
- `blocked`：v0.25.0 尚未在当前用户 Chrome 页面更新后复核；默认隔离抖音探针仍停在验证码中间页。用户报告的微博页面最终补位结果需要更新脚本并刷新后确认。

### 限制

微博补位只处理可识别的绝对定位虚拟行，并只对“行自身仅包含被隐藏评论”的情况做偏移补偿；canvas、不可变更位置的其他虚拟列表实现仍需单独适配。

## 0.24.0 - 2026-08-24

### 抖音评论入口与微博补位修复

- **抖音评论单条入口**：适配当前评论“三个点”打开的 `role="tooltip"` portal，普通文本形式的
  「举报评论」旁现在也会插入「🚫 本地拉黑」，并继续复用刚打开的评论身份。
- **抖音评论批量入口**：评论详情侧栏的 `#relatedVideoCard.LookModalFrameFast` 虽然类名含
  `Modal`，但它本身是评论承载层；脚本不再因此隐藏左下角「🚫 抖音评论屏蔽(N)」入口。
- **微博评论补位**：只包住被屏蔽评论的虚拟包装器会被压为真正的零高度；即使前端以内联固定
  高度/内边距复写包装器，撤销时也会恢复原样。

### 验证边界

- `real-site verified`：2026-08-24 当前用户浏览器中的抖音页实际捕获到 23 条评论、`role="tooltip"`
  举报项和 `#relatedVideoCard.LookModalFrameFast` 评论承载层；微博隔离公开详情页实际观察到
  楼中楼隐藏后根评论从 139px 上移到 87px，撤销恢复。
- `structure regression`：`node test/adapters.cjs` 21/21、`node test/run.cjs` 12/12、
  `node test/state.cjs` 7/7、`node test/quickblock.cjs` 32/32、`node test/douyin.cjs` 2/2；
  `node --check omniblock.user.js` 与 `git diff --check` 通过。新增真实抖音 tooltip/详情侧栏夹具
  和固定高度微博虚拟包装器夹具。
- `blocked`：v0.24.0 尚未在用户当前登录浏览器安装后复核；默认抖音探针仍停在验证码中间页，
  用户所说的微博具体空洞场景也尚未在同一真实页面执行脚本本地拉黑操作。

### 限制

微博方案只对脚本能够安全识别为“仅包含被隐藏评论”的包装器强制零高度；canvas、绝对定位或
不接受高度变化的虚拟列表仍需要真实页面单独复核。

## 0.23.0 - 2026-08-24

### B站加载回归修复

- **修复页面主体加载失败**：B站视频/动态作者按钮改为挂在 `body` 的独立门户并贴近作者行，
  不再把节点插进 B站 Vue 管理的容器，避免评论组件因 `HierarchyRequestError` 中断渲染。
- **收窄弹幕网络桥接**：检测到 PAKKU 时只桥接 `pakku_open` 的弹幕回调，不再覆盖所有普通
  XHR；评论、图片和视频分片继续走页面/扩展原有链路。
- **新增回归保护**：聚焦测试覆盖普通 fetch、XHR、PNG 图片请求，以及 PAKKU 先安装时的
  弹幕过滤兼容。

### 验证边界

- `real-site verified`：2026-08-24 隔离未登录、自动发现的公开 `bilibili.com/video/...` 页
  注入 v0.23.0 后无页面错误；观察到 2 条根评论、2 个楼中楼容器、4 位评论作者、可见批量入口，
  播放器 readyState 为 4，弹幕 fetch/XHR 两条路径均到达页面。
- `structure regression`：`node test/quickblock.cjs` 32/32；新增普通 fetch、XHR、图片请求
  和 B站作者门户断言，原有弹幕、评论、批量、UID 候选及 PAKKU 夹具继续通过。
- `blocked`：用户当前 Chrome 中仍是旧版 v0.22.0，且本轮没有在该已安装 PAKKU/字幕扩展的
  浏览器会话里安装 v0.23.0 后再做登录态功能验证；需更新脚本后刷新复核。

### 限制

B站作者入口现在是独立定位的门户，作者行发生大幅布局变化时按钮会隐藏或贴到视口边缘；
未解析到可靠 UID 时仍不注入。普通资源不再进入弹幕 XHR 过滤分支，但真实 PAKKU/字幕扩展
组合仍需用户更新到本版本后复核。

## 0.22.0 - 2026-08-24

### 新增与修复

- **抖音弹幕按钮位置**：按钮改到弹幕文字左侧、与文字垂直对齐并保留少量重叠，鼠标从弹幕
  移向按钮时不会因为离开浮窗而立即消失。
- **抖音评论单条入口**：评论“三个点”打开的 portal 菜单中，「举报评论」旁会增加「🚫 本地拉黑」；
  脚本复用刚打开的评论上下文，身份不可靠时不注入。
- **抖音评论管理器**：左下角「🚫 抖音评论屏蔽(N)」打开后自动尝试展开带明确回复语义的子评论，
  统一显示已加载的根评论/子评论作者，可全选或批量屏蔽；不调用平台私有接口，也不把未加载内容
  猜成全量。
- **微博评论无空洞**：被屏蔽的普通评论和回复会连同只包住它的虚拟列表包装层一起收缩，后续
  评论补位；撤销时包装层恢复。弹窗回复路径同样有结构回归覆盖。
- **设置面板**：名单按平台分组；已有评论/弹幕依据的记录显示备注，鼠标悬停名单行可查看
  「屏蔽依据」。

### 验证边界

- `real-site verified`：2026-08-24 隔离未登录、自动发现的公开 `weibo.com/...` 详情页实际观察到
  一条楼中楼本地屏蔽后 `hidden=true`、正文保持可见、根评论保持可见，根评论位置从 191 上移到
  139，撤销后恢复。
- `structure regression`：`node test/adapters.cjs` 21/21、`node test/run.cjs` 12/12、
  `node test/quickblock.cjs` 31/31、`node test/state.cjs` 7/7、`node test/douyin.cjs` 2/2；
  `node --check omniblock.user.js` 和 `git diff --check` 通过。新增夹具覆盖抖音弹幕位置、评论
  portal 菜单、自动展开/批量管理器、微博后续虚拟回复上移和设置分组/依据提示。
- `blocked`：2026-08-24 默认隔离抖音探针被验证码中间页拦截，没有评论或弹幕候选；本轮没有获
  用户授权运行登录态探针，因此抖音评论菜单、评论管理器和新按钮位置不声称已在真实登录页验证。

### 限制

抖音管理器只会点击页面上带明确“展开/查看/更多/共 N 条回复”语义的可见控件，并限制最多尝试
80 个入口；平台未加载或没有可靠展开语义的评论仍不会进入列表。微博虚拟列表存在前端变体，
本轮线上只验证了普通未登录详情页的一条回复，展开弹窗和更深层回复以结构回归为准。

## 0.21.0 - 2026-08-24

### 新增

- **B站内容作者快捷入口**：视频页作者名旁、动态详情页作者模块内显示「本地拉黑作者」；
  视频作者使用当前空间链接 UID，动态作者使用页面作者状态中的可靠 mid，身份不可解析时不注入。
- **微博帖子作者快捷入口**：正文作者行和旧版信息流卡片作者名旁显示「本地拉黑作者」，
  与评论入口使用同一套本地名单和确认/撤销流程。

### 验证边界

- `real-site verified`（2026-08-24，隔离未登录会话，脱敏页面 `weibo.com/...`）：公开详情页
  观察到帖子作者「本地拉黑作者」入口；页面加载 22 条评论，其中 17 条根评论、5 条楼中楼，
  21 条解析出身份、26 个行内入口可见。
- `structure regression`：`node test/quickblock.cjs` 31/31、`node test/adapters.cjs` 20/20；
  `node --check omniblock.user.js` 与 `git diff --check` 通过。人工合成夹具覆盖 B站视频/动态作者
  和微博正文/旧版信息流作者入口的点击、确认、UID 屏蔽与撤销。
- `blocked`：本轮隔离未登录 B站公开视频页已发现但未加载真实评论组件（`commentRendererCount=0`），
  因此 B站作者入口未取得真实点击证据；探针还记录了页面 appendChild 错误，需后续在可加载评论的
  公开视频页复核。微博本次选中的回复虽能打开确认框并隐藏，但虚拟列表在撤销阶段回收根评论，
  未完成“根评论保持可见且撤销恢复”的完整事务；该部分不写成 real-site verified。

## 0.20.0 - 2026-08-23

### 新增

- **抖音弹幕本地拉黑入口**：把鼠标移到正在飘的一条弹幕上，弹幕上方会浮出「🚫 拉黑」
  按钮并随弹幕一起移动（弹幕层 `pointer-events:none`，但节点自身可交互）。点击后按
  `data-danmaku-user-id` 保存 `douyin:uid`，该弹幕和之后同一发送者的弹幕都会零占位
  消失，撤销可恢复。没有身份属性的弹幕不显示按钮、不会被误隐藏。
- **抖音作者弹幕映射**：`data-is-danmu-author="true"` 的作者弹幕会额外绑定当前视频
  作者（`[data-e2e="video-avatar"]` 的 sec_uid）；屏蔽作者本人后，作者自己的弹幕
  一并隐藏，无需在抖音数字 uid 和 sec_uid 之间做不可靠映射。

### 验证边界

- `real-site verified`（2026-08-23，用户已登录调试浏览器中的临时只读标签页，
  `douyin.com/...?...`；写入仅限注入的内存 GM 存储，标签页已关闭）：真实弹幕节点
  带 `data-danmaku-user-id`，悬停出现节点内的「🚫 拉黑」按钮，确认框包含正确的
  `douyin:uid`，点击后该弹幕隐藏、撤销恢复。
- `structure regression`：`node test/adapters.cjs` 19/19（新增作者弹幕身份契约与
  弹幕浮层 UI 闭环）、`node test/quickblock.cjs` 29/29、`node test/run.cjs` 11/11、
  `node test/state.cjs` 7/7、`node test/douyin.cjs` 2/2；语法检查与 `git diff --check`
  通过。
- `blocked`：隔离未登录浏览器打开 `douyin.com` 首页仍是「验证码中间页」，无候选条目；
  本轮验证视频没有渲染作者自己的弹幕（数量为 0），作者弹幕映射只由人工合成夹具覆盖。

## 0.19.0 - 2026-08-23

### 新增

- **B站批量拉黑精细化**：视频页左下角批量入口点击后先弹范围面板，可只处理「当前已加载」，
  或「加载全部评论与子回复」（只读分页抓取公开评论接口，包含未展开的楼中楼作者；任何一页
  失败都会提示“部分分页未取全”，不会伪装成完整名单）。可按「晚于该时间点」筛选发言
  （最近 1/6/24 小时、3/7 天或自定义时间），缺少可靠时间的记录自动跳过。
- **B站弹幕 UID 免二次确认**：`UID?` 查询得到唯一候选且已通过用户卡片正向校验时，按钮
  直接显示「拉黑本人」，点击后同时保存 hash 与 UID 并立即隐藏该 UID 的评论，不再弹确认框；
  同一文案存在多个存活候选（CRC32 碰撞）时仍保留「确认并拉黑」人工核对流程。

### 修复

- 批量范围面板自身是 `role="dialog"`，此前会被页面的 1.2 秒弹窗周期扫描当成原生弹窗而
  自动关闭；现在面板打开后保持稳定，只有真实原生弹窗出现时才隐藏入口。

### 验证边界

- `real-site verified`（2026-08-23，隔离未登录会话，目标自动发现或 `--url=` 指定）：
  B站公开 `bilibili.com/video/...` 页批量范围面板打开、跨过周期扫描，「仅当前已加载」
  模式完成 2 位真实评论作者拉黑/撤销；评论接口契约 `x/v2/reply/main` 匿名返回 code 0、
  3 条含 mid/ctime 的根评论，`x/v2/reply/reply` 返回 code 0、1 条子回复。弹幕 UID 两种
  路径均观察到：唯一候选直接拉黑无确认框、碰撞候选确认后合并，均完成撤销；弹幕工具
  26 组/30 位发送者单组与批量事务通过，浮动弹幕 11 条全部 `pointer-events: none` 且
  坐标命中拉黑/撤销成功。
- `structure regression`：`node test/quickblock.cjs` 29/29（含唯一候选免确认、碰撞候选
  确认、批量面板已加载/全量+时间筛选）、`node test/run.cjs` 11/11、`node test/adapters.cjs`
  17/17、`node test/state.cjs` 7/7、`node test/douyin.cjs` 2/2；userscript 与测试语法检查、
  `git diff --check` 通过。
- `blocked`：右侧弹幕列表在隔离会话仍未展开；完整「加载全部」分页翻页只在人工合成接口
  上验证，真实长评论视频的全量抓取未跑完一轮。

## 0.18.0 - 2026-08-23

### 修复

- **微博「共 N 条回复」弹窗**：展开后的根评论和楼中楼回复现在按各自作者身份挂载
  「本地拉黑」，回复被屏蔽时只收起对应行，不影响根评论；展开触发器本身没有作者身份，
  不会误注入入口。
- **B站弹幕原生操作条**：已登录且网站显示弹幕「举报」项时，脚本统一插入「🚫 本地拉黑」；
  同时补强无语义选项和短暂出现的菜单扫描。播放器浮动入口会持续跟随弹幕移动，目标离开
  视口或被回收时自动隐藏。
- **B站右侧弹幕列表**：按列表显示时间按需只读读取对应的 6 分钟 `seg.so` 数据段。唯一
  发送者显示「本地拉黑」，同文案多发送者显示「本地拉黑全部(N)」并逐 hash 确认；尚未
  读取或暂未匹配的行显示禁用状态，不伪造身份；隐藏时收起虚拟列表外层行，避免空行。

### 验证边界

- `real-site verified`（2026-08-23，隔离未登录会话，目标由探针自动发现）：B站公开
  `bilibili.com/video/...` 页渲染 3 条浮动弹幕，全部 `pointer-events: none`，坐标命中后
  浮层出现并完成 hash 拉黑/撤销；评论作者 4 位，弹幕工具 26 组/18 位发送者，单组与
  批量事务通过。微博公开 `weibo.com/...` 详情页 28 条评论中 27 条解析出身份，21 条根评论
  与 6 条作者不同于根评论的已加载楼中楼有入口；拉黑一条回复后根评论 110px → 63px 且
  保持可见，撤销恢复。
- `structure regression`：`node test/quickblock.cjs` 26/26、`node test/adapters.cjs`
  17/17、`node test/run.cjs` 11/11、`node test/state.cjs` 7/7、`node test/douyin.cjs`
  2/2；userscript 与测试脚本语法检查、`git diff --check` 通过。微博展开弹窗、B站列表
  行和原生举报选项均由真实捕获夹具覆盖，本轮没有将它们写成线上验证。
- `blocked`：本轮隔离未登录会话未展开 B站右侧弹幕列表，也看不到原生弹幕操作条；微博
  「共 N 条回复」弹窗没有出现可安全验证的目标，点赞/转发用户弹窗同样不可达。

### 限制

- 普通弹幕段不直接提供 UID；UID 反查仍只覆盖 1–10 位数字并可能遭遇 CRC32 碰撞，必须
  人工核对后才能保存 `bili:uid`。
- 本版本仍只做本地存储；`browser-use` 可用于登录态场景探索，但未安装、未用于本轮验证，
  也不能替代现有隔离只读探针。

## 0.17.0 - 2026-08-23

### 新增

- **本地自动快照**：名单写入现在会同步保存到独立的 `omniblock:backup:v1` 本机存储键，
  默认保留最近 5 份；设置面板可以关闭自动快照、查看数量并恢复上一份。恢复前会先保留
  当前状态，误恢复也可回退。
- 快照使用版本化的 `omniblock.snapshot` envelope；当前没有云端 provider 或上传请求，
  未来同步可在同一快照边界上接入而不改动主名单键。手动「导出 JSON」仍保持旧版 v1
  文件格式，已有离线备份可以继续导入。

### 限制

- 自动快照与主名单一样属于当前浏览器/Tampermonkey 配置。浏览器配置整体丢失时，仍需用
  设置里的「导出 JSON」保存到下载目录或其他设备。
- `structure regression`：本地状态与设置回归覆盖快照上限、恢复、关闭开关和 provider 形状；
  本版本尚未启用云同步。

## 0.16.0 - 2026-08-22

### 变更

- **B站播放器弹幕拉黑改为坐标命中**：把鼠标移到正在飘的弹幕上，弹幕旁会浮出
  「🚫 拉黑该弹幕发送者」。上一版依赖 B站弹出自带的弹幕「举报」菜单再往里插一条，
  但真实播放器的弹幕层写死 `pointer-events: none`，未登录会话下那条菜单根本不出现，
  上一版所依赖的 DOM 结构在当前线上页面也已不存在。现在脚本按指针坐标判断指向哪一条
  弹幕，不再依赖网站是否弹出操作条；已登录用户看到原生菜单时，那条入口仍复用同一身份。

### 修复

- **微博楼中楼终于都能拉黑**：真实详情页的楼中楼是 `.item2 > .con2 > .info > .opt`，
  没有上一版假设的 `.item2in` 中间层，导致大部分回复行拿不到挂载点。修复后真站上
  19/19 条根评论与 6/6 条已加载楼中楼都有行内入口（此前同类页面只有约一半）。
- 「共 N 条回复」这类展开行虽然也匹配 `.item2`，但没有作者身份，不会再被当作评论行。

### 验证边界

- `real-site verified`（2026-08-22，隔离未登录会话，目标由探针从公开入口自动发现）：
  B站页渲染 10 条浮动弹幕且全部 `pointer-events: none`，坐标命中后浮层出现、拉黑写入
  `bili:dmhash:` 并可撤销；同轮评论零占位隐藏、弹幕工具 100 组/102 位发送者的单组与
  两组批量事务均通过。微博详情页展开楼中楼后 19/19 根评论与 6/6 楼中楼均有入口，
  拉黑一条与根评论作者不同的回复后该行消失、根评论高度由 162px 收到 110px 且保持可见，
  撤销后恢复。
- `structure regression`：`node test/quickblock.cjs` 24/24、`node test/adapters.cjs` 16/16、
  `node test/run.cjs` 11/11、`node test/state.cjs` 6/6、`node test/douyin.cjs` 2/2。
- `blocked`：知乎被重定向到登录页；贴吧落在百度安全验证滑块页；X 未登录只有登录页；
  抖音首页没有可解析条目。未登录会话看不到微博点赞/转发用户列表弹窗，也不会弹出 B站
  自带弹幕操作条，这两条路径仍只有夹具证据。

## 0.15.0 - 2026-08-22

### 新增

- B站播放器内悬停一条弹幕并打开原生「举报」菜单时，菜单里会多出「🚫 B站弹幕发送者」，
  不必再去右下角工具或弹幕列表里找同一条。只有该弹幕的文案与显示时间在已解析数据段中
  唯一对应一个 `mid_hash` 时才注入；同一秒内有多条相同文案时视为身份不唯一，不注入。
- 微博楼中楼「本地拉黑」覆盖旧版与懒加载结构：`node-type="reply_list"`、`.list_ul`、
  `.WB_reply` 下的 `.item2` 行现在也会在自己的操作区出现入口，使用该行自己的 UID。
- 微博点赞、转发、粉丝等用户列表弹窗里，即使只列出一位可解析用户，也会显示
  「🚫 拉黑全部(N)」；弹窗关闭后复用时会先清掉上一次的按钮再重新统计。

### 修复

- 微博评论被屏蔽后不再留下空白：被隐藏行自身的包装层（只包含该行、没有其他内容的
  祖先节点）会同时收掉高度和内外边距，后面的评论直接补位。
- 微博根评论的名单标签不再为空：作者槽按优先级分组解析，优先取带昵称的作者链接，
  头像链接只作兜底，正文中的 `@提及` 不参与。
- 弹幕文案匹配改为按显示粒度（±1 秒）判定唯一性，避免同一秒内多个发送者被误当成
  同一人；这类情况改由按文案分组的批量流程处理。

### 验证边界

- `structure regression`：`node test/quickblock.cjs` 24/24（含悬停举报菜单唯一身份注入与
  同秒歧义不注入）、`node test/adapters.cjs` 16/16（含微博旧版楼中楼入口、包装层收起、
  单人弹窗批量与撤销）、`node test/run.cjs` 11/11、`node test/state.cjs` 6/6、
  `node test/douyin.cjs` 2/2。
- `blocked`：本轮没有获得可用于只读探针的真实 B站/微博页面 URL（探针现在要求显式
  `--url=`），因此悬停弹幕举报入口、微博旧版楼中楼与弹窗批量都未在生产站点复核。
- 公开文档、变更说明和测试不再包含具体验证页面标识，只保留平台域名与占位符。

## 0.14.0 - 2026-08-21

### 新增

- B站弹幕分组新增 `UID?`：按 `mid_hash` 反查 1–10 位数字 UID 候选，再匿名请求 B站
  用户卡片接口剔除不存在账号。候选始终标为「可能发送者」，并提供主页链接供人工判断。
- 用户确认某个候选后，名单把该弹幕 hash 与所选 UID 记录为同一人物；后续既过滤对应
  hash 的弹幕，也隐藏该 UID 的评论。仅查询、只按 hash 拉黑或未确认候选都不会写入 UID。

### 变更

- 项目整体许可证从 MIT 改为 GPLv3-only，以许可兼容方式采用 PAKKU 的 GPLv3 UID 反查
  实现；Pynseq-Weibo 与 Pynseq-Douyin 的原 MIT 版权和许可声明继续保留在
  `THIRD_PARTY_NOTICES.md`。
- userscript 新增 `api.bilibili.com` 连接权限。只有用户主动查询 UID 候选时才使用，
  请求不携带登录 Cookie，也不发送本地黑名单或原始浏览数据。

### 验证边界

- `real-site verified`：2026-08-21 隔离未登录访问
  `https://www.bilibili.com/video/...`；真实弹幕 hash 产生数字候选，匿名用户卡片
  校验后展示「可能发送者」。在隔离内存名单中确认候选后 UID 与 hash 同时命中，撤销后
  两者同时恢复；同轮原有弹幕单组、两组批量和评论无占位隐藏也通过。
- `structure regression`：22 项 B站聚焦回归覆盖单候选确认、CRC32 多候选逐个账号校验、
  不存在账号剔除、查询不写 UID、手动关联、设置名单昵称/UID/hash 与整体撤销。
- `blocked`：普通弹幕协议仍不直接提供 UID。反查只覆盖 1–10 位数字 UID，CRC32 碰撞和
  超过 10 位的新账号无法自动判定真实发送者；真实 PAKKU 扩展共存仍未在隔离探针安装。

## 0.13.0 - 2026-08-21

### 新增

- 微博当前详情页的每条已加载评论增加常驻「本地拉黑」入口；页面批量入口同时统计
  发帖人、根评论和已展开回复作者，并按 UID 去重。
- GitHub 版本发布流程：每个用户可见版本维护本文件、同版本 tag 和 Release；
  Tampermonkey 继续使用 raw `master` 自动更新。

### 修复

- B站弹幕工具改为按文案分组。相同弹幕来自多个发送者时，单组按钮会屏蔽组内全部
  发送者；勾选批量会展开所有选中组并按 hash 去重，不再只处理其中一人。
- B站原生弹幕列表的行内「本地拉黑」改为右侧固定操作区，避免与时间和文案重叠。
- B站弹幕名单明确显示 `mid_hash`、持续过滤作用，以及弹幕段不提供可靠昵称/UID 的限制；
  不会从不可逆 hash 反向猜测 UID。

### 验证边界

- `real-site verified`：隔离未登录微博详情页验证 6 条根评论入口、7 位已加载微博/评论
  作者批量统计、单条隐藏和撤销；隔离未登录 B站页实际捕获 6 组/7 位发送者，其中一组
  含 2 人，并验证该组两人单击屏蔽与两组展开为 3 人的批量事务。
- `structure regression`：两位发送者的相同弹幕聚合、批量展开去重、hash 身份边界、
  原生列表布局、微博楼中楼和 PAKKU 双注入顺序。
- `blocked`：B站原生弹幕列表没有可安全识别的发送者行，隔离探针未安装真实 PAKKU；
  评论数据早前两次停在加载 spinner（最终复核成功，仍保留间歇受阻记录），微博真实页
  没有展开楼中楼。

## 0.12.1 - 2026-08-21

- B站弹幕入口在零数据时常驻，支持主动读取首段、失败重试和 SPA 会话隔离。
- 按 PAKKU 公开 XHR 契约兼容其先于或后于 OmniBlock 注入的顺序。

## 0.12.0 - 2026-08-21

- 修复 B站楼中楼评论独立身份与零占位隐藏。
- 新增独立 B站弹幕发送者工具、单条/勾选批量屏蔽和撤销。

## 0.11.1 - 2026-08-21

- 评论和弹幕改为无提示、零占位隐藏；B站根评论隐藏完整线程，楼中楼只隐藏自身。

## 0.11.0 - 2026-08-20

- 加固六平台身份规范化、状态可逆、批量事务、微博/抖音/贴吧适配边界和测试运行时。

## 0.10.0 - 2026-08-20

- 修复 B站真实评论菜单、批量计数和 `mid_hash` 弹幕过滤路径，建立三类验证标签。
