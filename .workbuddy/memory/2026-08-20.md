# 2026-08-20

## OmniBlock 跨平台本地黑名单用户脚本（E:\pluginforchrome）

- **形态拍板（用户 2026-08-20）**：统一 Tampermonkey 用户脚本（方案 A），非 MV3 扩展。直接并入 Pynseq 抖音/微博（MIT，行为级复用），自建 B站/知乎/贴吧/X。
- **M0→M6 全部落地并 git 提交**：`f4b318d`(M0 地基) → `017d035`(M1-M6 全量) → `fbcf840`(把 vendor/ 移出交付仓库)。
- **关键决策**：
  - 抖音推荐流 skip 是唯一切拟操作，四道安全阀；绝不写 `media.muted`（污染全局静音偏好，Pynseq 踩过的坑）。
  - 所有拉黑入口自建 UI，绝不触发平台原生"不感兴趣"。
  - B站弹幕 MAIN world 拦截 seg.so + 手写 varint + CRC32 正向映射（无需彩虹表）。
  - 抖音/微博适配器脱胎自 Pynseq，已在 LICENSE 保留原作者版权与 MIT 署名。
- **未验证风险**：各平台 DOM 选择器需浏览器实跑验证/微调（平台会改版）；抖音网页弹幕若某版本不带发送者属性则自动跳过（计划 M5 已注明兜底）。
- vendor/ 仅作研究参考，已 .gitignore 排除，不进交付仓库。
- **自动更新已配置（2026-08-20）**：公开仓库 `a2787/ub-utils`（GitHub 账号 a2787，由 gh CLI 登录态创建），master 分支；脚本头 `@updateURL`/`@downloadURL` 指向 `raw.githubusercontent.com/a2787/ub-utils/master/omniblock.user.js`。`@name` 改为低调名「本地内容过滤增强」、`@namespace` 改 `a2787/ub-utils`、`@description` 去显眼词；出处署名保留。commit `a7676cb`。jsDelivr 抓取 GitHub 失败故用 raw 直连。
