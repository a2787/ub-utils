# OmniBlock v0.53.0（本地候选，未推送）

日期：2026-09-09
状态：本地源码候选，未 push、未创建 tag/Release、未部署或执行平台写入。
构建：`0.53.0-ai-background-bili-commit`
计划：`OB-AI-001`（B站 AI 交互）与 `OB-AI-011`（事实核查边界）

## 用户可见变化

- AI 提示词把“是否命中屏蔽规则”和“事实是否得到核查”拆成独立字段：`claimType`、`verificationStatus`、`verificationMethod`、`ruleMatched`。
- 没有引用、没有检索结果、模型暂时不知道、单句事实陈述或语境不足，不再单独构成“虚假/谣言”的屏蔽理由。旧响应即使返回 `decision=block`，只要理由落在“未经证实/无可核实依据”等保守边界，也会被客户端降为延后，不进入可勾选候选。
- 规则性攻击、广告等非事实性违规继续进入原有人工审核；有明确正面矛盾依据的事实性结果仍可进入人工审核，最终仍需用户确认才写入本地名单。
- AI 状态和审核弹窗会显示因事实/语境尚未核查而保留的数量，以及可执行候选的核查状态/依据。
- B站 AI 弹幕确认后审核弹窗立即关闭；基础 hash/已有 UID 先写入本地名单，hash→UID 按需关联由最多 2 个并发任务在后台补充，反馈记录和完成提示随后完成。唯一 UID 校验失败或存在碰撞时仍只保存已有 hash/UID，不扩大可执行范围。
- AI 分析日志新增 `runId`、采集耗时、网关耗时和总耗时；只有存在实际运行/请求/加载器时才记录取消事件，手动分析期间到达的新内容合并到后续自动增量批次。

## 设计边界

当前 userscript 只向用户配置的 loopback Chat Completions 网关发送截短正文；本候选没有增加公共搜索、浏览器外发或新的扩展权限，不把模型内部记忆冒充外部来源。真正的联网事实核查需要另立检索通道，明确来源可信度、隐私、缓存、失败回退和成本后再实现。

关键词优先级、人工审核、反馈账本、身份键、B站 hash→UID 按需关联、80 条批次和平台写入边界均保持不变。

## 验证证据

- `structure regression`：`node test/ai-prompt-eval.cjs` 5/5、`node test/ai-prompt-system.cjs` 13/13、`node test/ai-screening.cjs` 21/21；其中 700ms UID 卡片延迟夹具确认审核弹窗先于后台识别关闭，基础 hash 立即生效，随后完成名单写入并提示；userscript 语法、B站 quickblock、内容 AI、多平台、批次、自动加载、适配器和通用运行器回归通过，页面/控制台错误为 0。
- `real-site verified`：2026-09-09 用户授权的专用 Chrome 只读页面，B站页面形式 `bilibili.com/...` 的 0.53.0 bridge 为 ready，实际分析 `8/8` 条、屏蔽候选 `0`、延后 `1` 条；贴吧页面形式 `tieba.baidu.com/p/...` 的 0.53.0 bridge 为 ready，实际分析 `17/17` 条、候选 `2` 条，候选理由中的“未经证实/无来源/无法核实”计数为 `0`。未点击平台举报、官方拉黑、关注或发帖控件。
- `real-site verified`：同日最后一次 B站只读维护探针动态发现 `bilibili.com/video/...`，读取 1 条作品、4 条评论、64 条弹幕，共 69 条 AI 记录（69 条带身份），错误 `0`；这只证明读取/分析链路，不证明模型精度或平台写入。
- `real-site verified`：同日专用 Chrome 维护探针确认 B站、抖音、微博、知乎、贴吧入口页面均运行 0.53.0 且 bridge ready；详情页未响应或 X 入口无语义内容的部分按 `blocked` 保留，未将入口计数扩写为详情全量。
- `blocked`：本候选尚未接入外部检索，不能声称已经对任意平台事实完成独立核查；当前只允许 loopback 网关返回显式核查字段，不把模型内部知识冒充来源。
- `real-site verified`：2026-09-09 真实 B站视频 DOM 上运行 `--verify-ai-background`，隔离 loopback mock 选出 1 条弹幕候选；脚本自己的确认操作后审核层立即消失、基础 hash 在第一次隔离存储写入中生效，随后出现完成提示；UID 卡片 mock 返回失败时保留 hash。该项证明真实 DOM/事件生命周期，不证明模型精度，也未执行平台官方写入。
- `blocked`：真实模型误杀/漏识别精度、外部事实检索和 B站评论分页/动态 UID 仍不在本候选的可验证范围；探针未执行平台举报、官方拉黑、关注或发帖。

## 发布状态

本条目仅记录本地候选；未 commit/push/tag/Release/部署。公开发布前必须重新审阅真实模型输出、误杀/漏识别样本、源码哈希和隐私门禁，并取得当轮发布授权。
