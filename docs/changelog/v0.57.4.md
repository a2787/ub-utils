# OmniBlock v0.57.4：撤销接管右键的本地拉黑菜单

日期：2026-09-13
状态：本地候选（发布需当轮授权）

## 用户可见变化

- **右键完全交还页面**：删除"命中评论/帖子条目时接管右键并弹出自建菜单「🚫 拉黑此用户」"的整套实现。此前右键落在已捕获条目上会被 `preventDefault` 并替换为插件的拉黑按钮，使平台原生右键菜单（复制链接、在新标签页打开、平台的"不感兴趣"等）在这些位置失效；现在右键、平台原生菜单与页面自身的上下文操作全部保持原样。
- **本地拉黑入口不变**：平台原生菜单（三点或「更多」→「举报」「加入黑名单」）旁的「🚫 本地拉黑」、确认气泡、撤销 toast、B站弹幕工具、批量屏蔽与作品级屏蔽全部保留；右键不再是任何功能的入口。
- 快捷入口在身份无法识别时的提示不再引导用户"试右键"，改为"请重新打开菜单后重试"。

## 实现边界

- 删除范围：`document` 级 `contextmenu` 捕获监听、`buildContextMenu`、仅供该监听使用的 `findItem`（沿 `Event.composedPath` 命中条目）、`#ob-ctx` 样式与相关注释。`showConfirm`、`Store`、各平台适配器、身份键规范与隐藏链路均未改动。
- 不新增替代手势或新入口；不改动存储格式、同步服务、账户数据或平台写入边界。
- 本地拉黑仍然只写本地名单，绝不触发平台官方拉黑、举报、关注或发帖。

## 验证证据

- `structure regression`：`node test/run.cjs` 20/20，页面/控制台错误 0。C 用例断言"在 B站评论（Shadow DOM 内）派发 `contextmenu` 既不 `preventDefault` 也不出现 `#ob-ctx`，同时身份仍可解析为 `bili:uid:333`"；该断言在旧源码上以 `{"defaultPrevented":true,"ctxShown":true}` 失败，删除实现后转通过。
- `structure regression`：`node test/adapters.cjs` 28/28，错误 0。改写贴吧嵌套正文目标与 B站 Shadow DOM 两处用例为"右键保持原生"，二者在旧源码上同样以 `defaultPrevented: true` 失败后转通过；六个平台其余 26 项（知乎/贴吧 portal 菜单、微博帖子/楼中楼/点赞弹窗、抖音弹幕与评论工具）保持通过。
- `structure regression`：本地拉黑全流程仍在 `run.cjs` D 用例覆盖——点开平台"更多"挂载评论菜单 → 原生菜单旁快捷入口 → 确认气泡 → `bili:uid:333` 进名单且评论隐藏，证明撤销右键没有破坏拉黑链路。
- `structure regression`：`node test/quickblock.cjs` 38/38（含真实 `bili-comment-menu #options` 注入、"硬核会员举报"菜单、楼回复入口、弹幕工具与批量撤销）；`node test/userscript-product.cjs` 7/7。
- `structure regression`：与 CI `maintenance` 同一组本地矩阵 21/22 脚本退出 0（含 state、comment-manager、danmaku-auto、work-block、performance、weibo-replay、douyin、sync、sync-server、ai-platforms、content-ai、content-coverage、ai-autoload、ai-watchdog、probe-hygiene、ai-prompt-system、ai-prompt-eval）。
- `blocked`：本轮未运行真实站点探针。用户 2026-09-12 已决定备考期间暂停全部实页验证；因此"真实网站右键菜单不再被接管"目前只有夹具证据，未取得 `real-site verified`。
- 未决（非产品回归）：`node test/ai-screening.cjs` 的 25 项断言全部通过、`FAIL: 无`，但脚本以非零退出——其退出码把任何控制台错误都记为失败，而 AI-24 用例自身 mock 的 HTTP 429 在当前 Chrome 上被记为一条资源加载 console error。该噪声与本轮改动无关，需要单独一轮修测试退出判据。

## 安装与回滚

- `@updateURL`/`@downloadURL` 指向 raw master；发布后 Tampermonkey 下次检查更新自动升级。
- 回滚仅需恢复 v0.57.3（右键接管会一并回来）；用户名单、设置、规则与 Key 保留，本版本没有修改存储格式、同步服务或账户数据。
