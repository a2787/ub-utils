# 2026-08-30 v0.46.0 已关闭计划

状态：只读归档。当前未关闭事项以 [`../PLAN.md`](../PLAN.md) 为准。

本归档保存本轮已经完成并有证据支撑的计划项，避免把历史继续堆积在唯一活动计划中。证据数字对应
2026-08-30 的候选源码；版本、提交和真实站点边界以 [`../CURRENT.md`](../CURRENT.md) 为准。

## OB-GOV-001 — 统一维护控制平面

- status: verified
- result: 规则、知识树、工作流、CURRENT、PLAN、架构正文和 ADR 各有单一职责；活动文档纳入 UTF-8 字节预算。
- evidence: `node test/docs-check.cjs` 通过；下一位维护者可从知识树路由到当前事实、活动计划和运行时边界。

## OB-GOV-002 — 可执行的状态与证据门禁

- status: verified
- result: 文档门禁检查计划字段/依赖/环、活动文档预算、版本/构建、内部链接、CURRENT 祖先快照、源码 SHA-256 和工作区描述。
- evidence: `node test/docs-check.cjs` 通过；隐私门禁和 `git diff --check` 纳入维护闭环。

## OB-RUN-001 — 开发扩展桥接的安全与可用性

- status: verified
- result: 构建期随机 HMAC、来源/单调序列、存储与只读网络白名单、有界 ready 降级；页面不再获得 `window.GM_*`。
- evidence: 持久开发扩展回归 4/4，覆盖三个新文档自动加载、跨页存储、伪造消息拒绝和 8 次后停止重试。

## OB-RUN-002 — 页面会话与 teardown

- status: verified
- result: `RuntimeResources`、共享路由信号、freeze/resume/BFCache/pagehide 契约和扫描器统一 teardown 已实现。
- evidence: 通用 UI/状态回归 19/19；SPA 路由资源数不增长，普通 pagehide 重复 dispose 后活动资源为零。

## OB-DATA-001 — 日志与名单的长期资源边界

- status: verified
- result: 被动高频扫描日志聚合，用户动作/错误逐条保留；Store/EventLog 报告体积与写入耗时，名单在 2 MiB/3 MiB 提示但不自动删除。
- evidence: 通用 UI/状态 19/19、性能边界 7/7；日志分片字符数缓存和写入失败降级有断言。

## 关闭边界

- 本归档不表示 `OB-PERF-001` 已完成：抖音真实页仍被验证码中间页阻断，真实 CPU 基线不得由夹具推导。
- 本归档不授权 CI/CD 修改、push、tag、GitHub Release 或生产分发。
