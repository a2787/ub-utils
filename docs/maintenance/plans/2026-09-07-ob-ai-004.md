# 2026-09-07 OB-AI-004 已关闭计划

状态：只读归档；当前未关闭事项以 [`../PLAN.md`](../PLAN.md) 为准。

## 目标与结果

- status: verified
- scope: 将当前页面已经观察到且通过现有身份/去重边界的全部 AI 记录按每批最多 80 条顺序送入 loopback 网关；合并各批模型结果后再进入同一个人工审核；抖音评论与弹幕 AI 正文只清理末尾平台控件词。
- result: 采集器不再把 80 条同时当作全页上限；文本预算仍按单批执行，网关请求仍遵守 80 条白名单。设置页显示总数、批次进度和“全部已观察内容”结果。抖音评论/弹幕清洗支持语义控件单词和连续控件后缀，正文自身出现同名词时保留。
- non-goals: 不自动展开或滚动页面，不把平台总数当作已加载数；不提高单请求网关白名单上限，不改变无身份出站、人工确认、名单写入、身份键或抖音私有接口边界；不执行 push、tag、Release、部署或平台写入。

## 验收与证据

- `structure regression`：`node test/ai-batch.cjs` 2/2 证明 85 条人工合成抖音评论按 `80+5` 两次请求、全局 ID 无重复、最终状态总数 85；`node test/ai-platforms.cjs` 6/6 覆盖评论/弹幕身份、正文控件后缀、正文同名词保留、换片隔离和普通属性变化；`node test/ai-screening.cjs` 10 项通过，原有 watchdog、人工审核和 loopback 边界未回归。
- `structure regression`：`node --check omniblock.user.js`、`node test/docs-check.cjs`、`node test/run.cjs` 20/20、`node test/state.cjs` 9/9、`node test/adapters.cjs` 28/28、`node test/quickblock.cjs` 36/36、`node test/danmaku-auto.cjs` 7/7、`node test/comment-manager.cjs` 3/3、`node test/work-block.cjs` 3/3、`node test/performance.cjs` 8/8、`node test/douyin.cjs` 2/2、`node test/dev-extension.cjs` 5/5 和 `node test/gateway-smoke.cjs --skip-docker` 均通过；页面/控制台错误为 0。
- `real-site verified`：2026-09-07，专用 Chrome 登录态会话运行最终候选 `0.48.0-ai-batched-content-cleanup` 的抖音只读探针，观察到 10 条带身份弹幕、10 条评论记录；10 秒稳定性采样心跳错误为 0，末次 DOM 为 25 条弹幕、100 条评论，入口、管理器、隐藏/恢复通过，未点击平台写入控件。
- `blocked`：真实页面本轮没有稳定超过 80 条并完成 AI 网关请求的样本，所以“超过 80 条的线上分批数字”不写成真实站点已验证；该数字由人工合成 85 条回归完成。抖音匿名入口验证码和同 URL 换片后缺少稳定新行的既有限制继续保留。

## 发布边界与回滚

- 候选仍为 `0.48.0`，构建为 `0.48.0-ai-batched-content-cleanup`；源码、网关和文档均未提交、未 push、未创建 tag/Release。
- 回滚只需恢复本轮源码/测试/文档候选差异；未改数据库、系统配置、provider 凭据或平台数据。
