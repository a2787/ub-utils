# OB-AI-007 — AI 网关重试超时预算对齐

- status: verified
- date: 2026-09-07
- scope: 对齐 userscript、持久化开发扩展 service worker 与本地 LiteLLM 重试窗口；当前网关每次请求 18 秒且允许 1 次重试时，客户端不得在约 20 秒先行中止。
- non-goals: 不修改 API Key、provider URL、模型或 `.env`；不改变 loopback 安全边界、批次大小、AI 输出协议或人工审核/名单写入；不把网关错误隐藏成成功。
- dependencies: OB-AI-001, OB-AI-005
- acceptance:
  - [x] userscript 单次 AI 请求预算覆盖当前网关 18 秒请求 + 1 次重试，并保留 watchdog 可观测失败。
  - [x] 持久化开发扩展 service worker 接受同一预算，不在 20 秒固定上限处截断。
  - [x] 人工合成 watchdog、桥接降级、批次和开发扩展回归通过。
  - [x] 专用 Chrome 登录态抖音页面重新完成当前已观察内容分析，审核态无超时且网关错误为空。
  - [x] docs、语法和 diff 门禁通过；不公开发布。
- evidence: `structure regression`：userscript 与 service worker 的 60 秒预算静态断言、`node test/ai-screening.cjs`、`node test/ai-bridge.cjs`、`node test/ai-watchdog.cjs`、`node test/ai-batch.cjs`、`node test/dev-extension.cjs` 均通过；`real-site verified`：2026-09-07 用户授权专用 Chrome 登录态 `douyin.com/video/...` 刷新候选扩展后，245 条当前已观察内容进入 4 批并完成 245/245，审核态 8 条候选，桥接 ready，网关收到 4 次 POST 且均为 HTTP 200，`lastError` 为空。
- next: 关闭本地候选计划；继续保留商汤 provider、抖音验证码和换片新行等独立限制，不公开发布。
- updated: 2026-09-07
- supersedes: none
- files: omniblock.user.js; test/build-dev-extension.cjs; test/ai-screening.cjs; test/ai-bridge.cjs; test/ai-watchdog.cjs; docs/maintenance/PLAN.md; docs/maintenance/CURRENT.md; docs/changelog/v0.48.0.md; README.md
