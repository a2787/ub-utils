# 2026-09-12 平板线封存记录（OB-TM-001 / OB-SYNC-001 设备验证部分）

状态：**封存（deferred），不是终止**。用户于 2026-09-12 决定："平板不是不做，而是
现在不做，等以后有机会再做"。维护重心回到电脑；本文件保存重启平板线所需的全部
上下文，重启时从这里的清单继续，不需要重新开发或重新设计。

## 封存时已交付并发布的状态

- v0.57.1 已公开发布：master 推送、annotated tag `v0.57.1`、GitHub Release（Latest）、
  raw `@updateURL` 实测返回 `0.57.1`；发布说明区分三类证据。
- 所有本地回归绿：product 6/6、通用 20/20、B站 38/38、适配器 28/28、同步核心 7/7、
  Python 服务 5/5、weibo-replay 13/13；维护总检本地项零 FAIL。
- 2026-09-11 B站/微博匿名隔离只读探针通过（页面/控制台错误 0）。
- 东京独立 `omniblock-sync` 服务保持部署在线（loopback 与 Quick Tunnel health 200）。

## 重启平板线时的待办清单

1. 平板 Edge 的 Tampermonkey 更新到 0.57.1（raw 更新地址已可用，可手动"检查更新"）。
2. 实测无 hover 入口常驻可见、齿轮单点进设置、面板/审核浮层 44px 触控目标
   （OB-TM-001 最后一项验收，结果记 `real-site verified` 或 `blocked`）。
3. 填真实 provider 地址与本机 Key 做一次 AI 直连验证。
4. 电脑与平板用同一账户、同一同步口令各点一次"立即同步（合并）"，回填密文读回与
   确定性合并证据（OB-SYNC-001 验收 2–4 项）。
5. 处理同步服务地址：当前 Quick Tunnel 重启会更换地址，重启平板线时确认入口是否
   仍然有效；长期使用应换固定域名或 Named Tunnel（需用户另行决策）。

## 恢复动作与边界

- 恢复入口：用户说"重启平板线"时，把 `PLAN.md` 的 OB-TM-001 / OB-SYNC-001 改回
  `in_progress`，按上面清单逐项执行并回填 CURRENT；本文件保持只读归档，不在其中
  追加新事实。
- 期间线上服务保持现状；不删除 `extension/` 归档，不回滚 v0.57.1。
- 红线不变：任何情况下不执行平台举报/官方拉黑/关注/发帖；API Key、密码、同步口令、
  令牌和日志不进入同步文档与本仓库。
