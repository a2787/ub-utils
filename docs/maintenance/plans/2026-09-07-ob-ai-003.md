# 2026-09-07 OB-AI-003 已关闭计划

状态：只读归档；当前未关闭事项以 [`../PLAN.md`](../PLAN.md) 为准。

## 目标与结果

- status: verified
- scope: 修复 AI 请求在 GM/XHR 桥接不回调时永久 loading；明确 80 条样本上限；清理抖音真实探针的 document-start 注入；让持久 MV3 开发扩展经 service worker 发起窄 loopback AI POST。
- result: userscript 增加独立 watchdog；加载态显示已观察数量和本轮样本上限；探针保存并移除 CDP 注入脚本标识；开发扩展 service worker 重新校验 loopback/窄 JSON 白名单后请求本机网关。
- non-goals: 没有扩大 AI 采集范围、改变人工审核/身份边界、配置商汤或公开发布；没有强制关闭用户旧标签页。

## 证据

- `structure regression`：`node test/ai-watchdog.cjs` 1/1、`node test/probe-hygiene.cjs` 1/1、`node test/dev-extension.cjs` 5/5、AI/平台本地回归和 docs check 通过。
- `real-site verified`：2026-09-07 用户授权专用 Chrome 新建抖音 `douyin.com/jingxuan` 标签，观察 24 条内容，手动分析从 loading 进入 `ready`，本地网关收到 POST 200；未点平台写入控件。
- `blocked`：匿名抖音入口验证码；旧用户标签页保留历史 probe 注入状态，未强制关闭；综合维护闭环的 B站加载/分页和微博顶层 spacer 条件仍按既有规则记录为 blocked。
