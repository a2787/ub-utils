# 2026-09-07 OB-AI-005 已关闭计划

状态：只读归档；当前未关闭事项以 [`../PLAN.md`](../PLAN.md) 为准。

## 目标与结果

- status: verified
- scope: 修复持久化开发扩展桥接降级时 AI 请求等待 20 秒后才报错的问题；区分 loopback 地址校验、网关请求失败和浏览器扩展桥不可用三类状态。
- result: `bridge.state !== ready` 时在 `requestJSON` 发起前快速失败，并提示刷新开发扩展和当前页面；有效 loopback 请求进入错误态时，设置页显示“loopback 地址校验已通过”，不再追加误导性的地址限制文案。原有请求 watchdog、AI 分批、人工审核、抖音正文清洗和 service worker loopback 白名单未改变。
- non-goals: 不读取或修改 API Key、`.env` 或 provider 配置；不改变网关路由、身份键、人工审核和平台写入边界；不执行 push、tag、Release、部署或平台写入。

## 验收与证据

- `structure regression`：`node --check omniblock.user.js`、`node test/ai-bridge.cjs`（桥接降级 2ms 内失败且有效 loopback 文案正确）、`node test/ai-watchdog.cjs` 1/1、`node test/ai-screening.cjs` 11 项、`node test/ai-platforms.cjs` 6/6、`node test/ai-batch.cjs` 2/2、`node test/dev-extension.cjs` 5/5、`node test/run.cjs` 20/20、`node test/adapters.cjs` 28/28 和 `node test/docs-check.cjs` 均通过；页面/控制台错误为 0。
- `real-site verified`：2026-09-07，专用 Chrome 当前抖音登录态页面重载后实际运行候选 `0.48.0-ai-batched-content-cleanup`；源码/扩展构建一致，`__OB_EXTENSION_RUNTIME__.bridge.state` 为 `ready`，未点击平台写入控件。此前同页实际错误日志记录 131 条内容、2 批分析、首批 0 条完成并在 20 秒处超时；网关日志同期无 POST，证明请求未离开浏览器桥接。
- `blocked`：修复后本轮抖音只读探针没有稳定渲染带发送者身份的弹幕，未形成恢复后真实 provider 请求样本；因此不宣称 DeepSeek 本轮请求或额度已重新验证。网关健康和模型列表此前已返回 200，故本次根因不升级为 provider 配置错误。

## 发布边界与回滚

- 候选仍为 `0.48.0`，构建为 `0.48.0-ai-batched-content-cleanup`；源码、网关和文档均未提交、未 push、未创建 tag/Release。
- 回滚只需恢复本轮源码/测试/文档候选差异；未改数据库、系统配置、provider 凭据或平台数据。
