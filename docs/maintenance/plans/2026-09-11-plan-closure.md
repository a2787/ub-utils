# 2026-09-11 计划收尾归档（11 项）

本轮对活动计划做一次性收尾：交付完成且有证据的项关闭；剩余工作被登录墙、验证码、
真实样本或用户设备/授权阻断的项，以 `blocked`/`deferred` 终态归档，不降低证据等级、
不把外部阻断写成通过。OB-TM-001 与 OB-SYNC-001 是 v0.57.1 当前交付线，仍留在
`docs/maintenance/PLAN.md` 活动区等待用户动作。每项残留均已在 `CURRENT.md` 的
blocked/partial 清单或对应版本条目中有落点。

## OB-EXT-001 — superseded

- 结论：平板 Edge 实际安装路径确认为 Tampermonkey userscript，MV3 交付主线停止。
- 交付与证据：同一源码可产 userscript/MV3、扩展端存储/AI 直连/账户同步原型均有本地回归；
  `extension/`、`test/product-extension.cjs` 与 `--product` 构建分支已归档提交（`cde7e44`），
  保留复用可能，未删除。
- 残留：平板扩展安装永不验证（不再作为交付路径）；线上 sync 部署改由 OB-SYNC-001 承接。

## OB-AI-001 — deferred（2026-09-12 用户决策后关闭）

- 结论：第一阶段交付（B站评论/弹幕 AI、规则/人工审核闭环、OpenAI 兼容 loopback 网关）
  已随 v0.47.0–v0.56.0 发布，真实页面证据见各版本条目；本项无未完成的交付范围。
- 暂缓原因：商汤 provider、额度、fallback 与 cooldown 评估需要用户提供凭据与成本决策，
  一直标记"另行评估"，不属于已交付范围。
- 恢复动作：用户决定接入商汤时另立计划项，按当轮授权评估。
- 2026-09-12 追记：用户澄清 AI 直连本就是 provider 无关设计（填写任意
  OpenAI-compatible API 即可），将使用官方 DeepSeek API 确保稳定性；商汤专项评估
  不再需要，v0.57.3 的「测试连接」按钮承担配置验证职责。本项就此关闭。

## OB-AI-002 — blocked

- 结论：B站/抖音当前页采集、活动视频会话隔离、一键网关启动已交付并取得真实页面证据
  （2026-09-07 抖音登录态 245/245 四批完成）。
- 阻断：抖音同 URL 换片后的新会话隔离需要稳定真实样本；匿名入口停在验证码中间页。
- 恢复动作：用户授权专用 Chrome 登录态探针后重放稳定换片并确认新会话隔离。

## OB-AI-004 — blocked

- 结论：可学习提示词系统已随 v0.52.0 发布——schema/迁移、三态反馈、提示词提案、
  离线评测、六平台内容入口均有 `structure regression` 与 2026-09-09 专用 Chrome
  `real-site verified`（B站 1/27/66、微博 6/1+6、知乎 12/3+展开 10、贴吧 1/11、抖音 45/1）。
- 阻断：X 根入口空壳无推文；抖音本轮未展开评论/弹幕；B站分页/动态 UID、微博 spacer、
  抖音换片与 DeepSeek 真实精度仍待真实样本，全部为外部条件。
- 恢复动作：随各平台登录态授权逐项补采；X 仍按用户要求排除。

## OB-AI-013 — blocked

- 结论："独立 AI 评测集与事实检索部署方案"的方案交付已完成：协议、门槛、来源 allowlist、
  rollout/回滚固化于 `plans/2026-09-10-ob-ai-013.md`，24 例离线评测与 broker/shadow/canary
  回归通过。
- 阻断：真实来源与精度/成本观察需要用户配置真实事实来源并另行授权接入；当前版本按规则
  不接入公共检索。
- 恢复动作：用户确认来源白名单与成本门禁后另立实施计划。

## OB-WEIBO-003 — verified

- 结论：详情页作品级评论统计作用域交付完成，全部验收项关闭。
- 补上的最后一项（虚拟行回收/复用下一帧重判 + 无屏蔽工作不建扫描）由
  `test/weibo-replay.cjs` 13/13 锁定：详情页滚动复用回放断言名单内作者在滚动帧内
  `data-ob-blocked` 且高度 0，空闲列表性能回放断言无屏蔽行时不触发整列表布局扫描；
  2026-09-11 微博真实探针中 virtualSync 诊断（4 次同步、2 次样式写回）与单条屏蔽/撤销
  恢复通过。真实长滚动全量行为仍按 `partial` 记录于 CURRENT，不扩大为全量结论。
- `structure regression`：work-block 3/3、weibo-replay 13/13、adapters 28/28；
  `real-site verified`：2026-09-04—09-05 与 09-11 的微博详情读取/隐藏/取消清理。

## OB-COVERAGE-001 — deferred

- 结论：作为 0.46.2 收口清单的目的已完成（v0.46.2 已发布）；B站行全部核验，微博主行
  （作者/主评论/楼中楼/管理器/作品级/恢复）有 2026-09-04—09-11 真实证据，知乎/贴吧/抖音
  有 2026-09-09 专用 Chrome 样本。此后的平台覆盖按各版本条目自带证据推进，本清单不再
  作为活动矩阵。
- 暂缓残留：微博点赞列表、知乎搜索/列表入口、贴吧旧版楼层的真实样本，以及抖音评论/弹幕
  展开与换片，均受登录墙/验证码阻断；X 按用户要求排除。
- 恢复动作：需要逐平台补采时，凭用户专用 Chrome 登录态授权按当前捕获结构重新核验。

## OB-PERF-001 — blocked

- 结论：可归因性能预算的测量框架、本地基线与回归阈值已交付（性能边界 8/8；B站等平台
  真实页面采样通过）。
- 阻断：抖音真实静置/播放/换片可归因基线受匿名验证码阻断，无法取得稳定目标。
- 恢复动作：用户授权专用 Chrome 登录态抖音会话后补采；期间不以夹具数字替代线上基线。

## OB-RULE-001 — deferred（2026-09-12 已重新启用并完成，见 PLAN 活动项）

- 结论：未开始实现。自动弹幕正则灾难性回溯的风险识别、保存前拒绝与编译缓存仍为
  proposed 时的原始范围。
- 暂缓原因：优先级 P2，且其自有下一步写明"必要时先以 warning 方式落地"；等待抖音
  基线与登录态条件以评估真实规则形态。
- 恢复动作：另立实施轮次；先补人工合成高风险/合法表达式回归，再谈启发式。
- 2026-09-12 追记：用户全面授权后本项已重新启用并在当轮完成实现、回归与发布
  （v0.57.2），终态见 `PLAN.md` 的 OB-RULE-001。

## OB-MAINT-001 — deferred（维持）

- 重复路径清理与受控模块化继续暂缓：等抖音真实基线解除 blocked 后先做调用方审计；
  没有明确收益时维持单文件发布物。无新增事实。

## OB-REL-001 — deferred（维持）

- CI/CD 仍待用户授权后新增最小工作流；候选三分类证据与 Release 门禁已落地。
  v0.57.1 的 push/tag/Release 属当轮授权事项，与 OB-TM-001 的发布决定一并处理。
