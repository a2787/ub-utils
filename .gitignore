vendor/
*.user.js.bak
test/_initscript.js
test/_initqb.js
test/_probe_real*.cjs

# test/ 下以 _ 开头的截图、日志和临时数据都是各轮真站探针与回归的运行产物，
# 按维护规则只作当轮排查用，不进入仓库。2026-08-22 至 08-24 曾逐批按名字登记
# （_real_/_shot_/_lastrun/_qb_/_dmrows/_wbmodal），现收敛为按类型的通配；
# 探针脚本本身仍按具体名字登记在下方，避免误伤可交付测试。
test/_*.png
test/_*.txt
test/_*.log
test/_*.json

# 2026-08-22 B站/微博真站诊断脚本；仅供当轮排查，不属于可交付测试。
test/_probe_dm_rows.cjs
test/_probe_wb_modal.cjs

# 上一轮 MV3 实验的专用调试构建目录；由 build-dev-extension.cjs 生成，不是当前 userscript 安装产物。
test/_dev-extension/

# 上一轮 MV3 实验的可安装构建目录；由 extension/build.cjs 生成。
dist/

# 独立同步服务的本地 SQLite/解释器缓存；只运行回归时生成，不进入仓库。
sync-server/*.sqlite3
sync-server/__pycache__/

# 2026-08-23 B站登录态/评论 API 诊断产物；仅供当轮排查，不属于可交付测试。
test/_cdp.cjs
test/_probe_comment_api.cjs
test/_probe_comment_structure.cjs
test/_probe_dy_danmu.cjs
test/_probe_dy_author.cjs

# 2026-08-24 作者入口一次性真站结构探针；仅供当轮排查，不属于可交付测试。
test/_probe_bili_dyn.cjs
test/_probe_bili_opus_state.cjs
test/_probe_bili_video_author.cjs
test/_probe_wb_author.cjs

# 2026-08-20 的一次性运行产物，与当前测试无关：run.cjs 现在只写忽略的
# test/_shot_*.png，这三张旧截图没有任何代码或文档引用。
test/shot-*.png

# 本地工作日志/助手缓存，不属于本仓库的可交付内容。
.workbuddy/

# 本地 AI 网关生成物；包含 API Key，不属于可交付源码。
gateway/runtime/
gateway/*.local.json
