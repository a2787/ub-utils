# OmniBlock 拉黑不上限（6 平台统一本地黑名单）

一个浏览器用户脚本：**一份本地黑名单，跨 6 个平台隐藏指定用户的所有内容**。
无数量上限、纯本地、不联网、不上传任何数据。

支持平台：**B站 · 微博 · 知乎 · 百度贴吧 · X(Twitter) · 抖音**（不含小红书）。

覆盖内容：
- 各平台评论区、帖子/动态、搜索结果、用户主页/作品
- **B站视频弹幕**（数据层 CRC32 过滤）
- **抖音视频弹幕**（网页弹幕带发送者属性时按 uid 隐藏）
- **抖音推荐流**：刷到被屏蔽作者 → 静音遮罩 + 自动切下一条（唯一一处"模拟操作"，带安全阀）

隐藏方式：折叠成灰条（默认，可追溯）／完全消失，设置里切换。

---

## 安装（傻瓜版）

1. 给浏览器装一个 **Tampermonkey**（油猴）插件：
   - Chrome / Edge：扩展商店搜 "Tampermonkey" 安装
   - 装好一次之后基本可以忘掉它
2. 把本项目的 `omniblock.user.js` 拖进 Tampermonkey：
   - 打开 Tampermonkey 管理面板 → 「实用工具」→「导入」→ 选 `omniblock.user.js`
   - 或把文件内容复制，新建脚本粘贴保存
3. 进任意支持的平台，右上角 Tampermonkey 图标 → 「OmniBlock 设置」即可管理名单、导出备份。

> 改一行刷新页面即生效，调试最直观。也可发布到 GreasyFork 分享给别人。

---

## 怎么用

- **右键**任意评论/帖子 → 弹出自建菜单「🚫 拉黑此用户」→ 确认。
- 鼠标**悬停**到评论上会出现「🚫 拉黑」小按钮。
- 设置面板里可手动按平台 + 身份值添加、删除、导出/导入 JSON 备份。
- 抖音推荐流刷到被屏蔽作者会自动跳过（可选关闭，改为只盖遮罩）。

### 各平台身份填写说明
| 平台 | 在设置里选 | 填什么 |
|---|---|---|
| B站 | B站 uid | 数字，如 `2233`（主页 URL `space.bilibili.com/2233`） |
| 微博 | 微博 uid | 数字，如 `1234567890` |
| 知乎 | 知乎 token | 个人页 URL `zhihu.com/people/这里` 的 slug |
| 贴吧 | 贴吧 uid | 数字 user_id（楼层 data-field 里） |
| X | X handle | 不带 @ 的用户名，如 `elonmusk` |
| 抖音 | 抖音 sec_uid | `MS4wLjAB...` 那串（作者主页 URL 里） |

> 最稳妥的方式还是**在网页上右键拉黑**——脚本会自动从页面里抓到正确的身份，不用你手填。

---

## 设计与风控要点

- **抖音推荐流自动切是唯一的"模拟操作"**，为防雷控设了四道安全阀：① 仅在命中黑名单时触发；② 同一视频只切一次（WeakSet）；③ 200–600ms 随机延迟；④ 连续跳过上限（默认 6 次）超限即停在遮罩不再自动切。设置里可一键降级为纯遮罩。
- **绝不写 `media.muted`** 来静音被跳过视频——抖音把静音当全局偏好，写它会污染你正常刷视频的静音状态（这是 Pynseq 踩过的坑，已规避）。改用视觉遮罩。
- **所有拉黑入口都是自建 UI**，绝不触发平台原生"不感兴趣"或官方拉黑，避免污染推荐模型。
- **B站弹幕**在页面拿到数据前拦截 `seg.so`，手写轻量 protobuf 解析 + CRC32 正向映射，把 `bili:uid:N` 算成弹幕 hash 比对，无需彩虹表。

---

## 致谢 / 许可证

- 整体 **MIT** 许可证。
- **抖音、微博**两个适配器的行为（选择器策略、`data-e2e` 锚点、推荐流跳过纪律、避开原生"不感兴趣"）脱胎自开源项目 **Pynseq-Douyin** 与 **Pynseq-Weibo**（作者 DanielZenFlow，**MIT**）。本项目按其 MIT 许可证保留了原作者版权与许可声明，并在此致谢。
- 其余 4 个适配器（B站/知乎/贴吧/X）及统一身份模型、隐藏引擎、弹幕钩子均为本项目自建。

---

## 状态与待验证

- 核心架构、共享名单、三种处置、拉黑入口、选项页、B站弹幕钩子均已实现并通过 JS 语法自检。
- **各平台的具体 DOM 选择器需在你自己的浏览器里实跑验证/微调**（平台会改版）。若某平台突然失效，通常是选择器过期，到对应 `Adapters.xxx` 里更新选择器即可，坏一家不影响其他家。
- 抖音网页弹幕若某版本不带发送者属性，则该平台弹幕过滤会自动跳过（已在计划 M5 注明兜底方案）。
