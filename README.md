# OmniBlock 拉黑不上限（6 平台统一本地黑名单）

一个浏览器用户脚本：**一份本地黑名单，跨 6 个平台隐藏指定用户的所有内容**。
无数量上限、纯本地、不联网、不上传任何数据。

支持平台：**B站 · 微博 · 知乎 · 百度贴吧 · X(Twitter) · 抖音**（不含小红书）。

覆盖内容：
- 各平台评论区、帖子/动态、搜索结果、用户主页/作品
- **B站视频弹幕**（数据段 `seg.so` 中按 `mid_hash` 过滤；评论 UID 也会经 CRC32 自动映射）
- **抖音视频弹幕**（网页弹幕带发送者属性时按 uid 隐藏）
- **抖音推荐流**：刷到被屏蔽作者 → 静音遮罩 + 自动切下一条（唯一一处"模拟操作"，带安全阀）

隐藏方式：折叠成灰条（默认，可追溯）／完全消失，设置里切换。

---

## 安装（傻瓜版）

1. 给浏览器装一个 **Tampermonkey**（油猴）插件：
   - Chrome / Edge：扩展商店搜 "Tampermonkey" 安装
   - 装好一次之后基本可以忘掉它
2. 把本项目的 `omniblock.user.js` 拖进 Tampermonkey：
   - 打开 Tampermonkey 管理面板 → 「实用工具」→「导入」→ 选 `omniblock.user.js`
   - 或把文件内容复制，新建脚本粘贴保存
3. 进任意支持的平台，右上角 Tampermonkey 图标 → 「OmniBlock 设置」即可管理名单、导出备份。

> 改一行刷新页面即生效，调试最直观。也可发布到 GreasyFork 分享给别人。

---

## 更新方法（以后加功能 / 修 bug 怎么换新版）

你拖进 Tampermonkey 的是一份**本地副本**，原稿改了它不会自己变。新版获取有三条路：

**① 一键检查更新（最省事，日常推荐）**
- 打开设置面板（右下角 ⚙）→ 点「检查更新」。
- 它自动去仓库拉最新脚本、比对版本号：
  - 有新版 → 弹出 Tampermonkey 的「更新」页，**点一次「更新」即装好**（用户脚本无法在运行时自替换，这一下点击不可避免）；
  - 没新版 → 提示「已是最新」。
- Tampermonkey 菜单里也多了一条「检查更新」，面板关着也能用。

**② 全自动（彻底免拖文件，建议设置一次）**
- 在 Tampermonkey 里把本脚本「更新 → 模式」设为「自动」。
- 之后 TM 每天例行检查时**静默应用**新版，你完全不用管，也不用手动点。
- 平时让它自己跑；想立刻看结果时用 ① 点一下。

**③ 手动覆盖（兜底）**
- 把新文件再拖一次进 Tampermonkey，版本号更高会弹「更新吗」，点更新即可；或编辑粘贴保存。
- 黑名单名单、设置都还在，不会丢。

> 关键点：我每次改完都会把脚本头 `@version` 数字加一位，Tampermonkey 才会认出「有新版」。这个我会自动做，你不用管。
> 仓库为公开 `https://github.com/a2787/ub-utils`，脚本头 `@updateURL`/`@downloadURL` 指向 GitHub raw 直连（jsDelivr 暂不可用，后续可切回以获国内加速）。仓库名/脚本名刻意低调（不含"拉黑/黑名单"显眼词），Pynseq 出处署名仍保留。

---

## 怎么用

- **右键**任意评论/帖子 → 弹出自建菜单「🚫 拉黑此用户」→ 确认。（B站评论/动态在 Shadow DOM 内，已做穿透，同样可用）
- 鼠标**悬停**到评论上会出现「🚫 拉黑」小按钮（浮层定位，B站也能显示）。
- 页面右下角有常驻 **⚙ 按钮**，点开就是设置面板（不再需要去 Tampermonkey 菜单里找）。
- 设置面板里有 **「检查更新」按钮**：点一下自动比对仓库版本，有新版会弹出 Tampermonkey「更新」页（点一次即装）。想彻底免拖文件，按「更新方法 ②」把 TM 更新模式设为「自动」即可。
- 设置面板里可手动按平台 + 身份值添加、删除、导出/导入 JSON 备份。
- 抖音推荐流刷到被屏蔽作者会自动跳过（可选关闭，改为只盖遮罩）。

### 各平台身份填写说明
| 平台 | 在设置里选 | 填什么 |
|---|---|---|
| B站 | B站 uid | 数字，如 `2233`（主页 URL `space.bilibili.com/2233`） |
| 微博 | 微博 uid | 数字，如 `1234567890` |
| 知乎 | 知乎 token | 个人页 URL `zhihu.com/people/这里` 的 slug |
| 贴吧 | 贴吧 uid | 数字 user_id（楼层 data-field 里） |
| X | X handle | 不带 @ 的用户名，如 `elonmusk` |
| 抖音 | 抖音 sec_uid | `MS4wLjAB...` 那串（作者主页 URL 里） |

> 最稳妥的方式还是**在网页上右键拉黑**——脚本会自动从页面里抓到正确的身份，不用你手填。

### 快捷拉黑（一键识别 + 锚定在原生按钮旁）

除了右键/悬停，脚本还会**自动在平台原生"拉黑/举报"按钮旁边**，插入一个「🚫 本地拉黑」入口——不用你手动找，点一下就识别并拉黑当前用户：

- **评论区 / 用户主页**：在平台自带的「拉黑」「加入黑名单」等菜单项**下方**，多出一个「🚫 本地拉黑」。点它，脚本沿 DOM 链（含 Shadow DOM）自动识别对应用户并弹出确认。
- **B站评论区**：当前页面的 `bili-comment-menu` 内会在「加入黑名单」后插入「🚫 本地拉黑」。只有脚本已从该菜单上下文解析出 UID 时才会插入，因此稿件举报框不会出现一个必然失败的按钮。
- **B站右侧弹幕列表**：脚本从弹幕段数据取 `mid_hash`，在能与列表行对应时显示「本地拉黑」。它保存的是 `bili:dmhash:<hash>`，不是不可逆地伪装成 UID；已加载到播放器的弹幕需在下一段加载或刷新后完全消失。
- 入口靠**菜单文字 + 可验证身份上下文**匹配；没有身份时宁可不显示，也不放一个点击后报错的入口。
- 两个开关都在设置面板里：`本地拉黑入口`（评论/用户/弹幕旁的小按钮）、`一键拉黑全部`（见下）。

> 为什么可靠：识别逻辑复用各平台适配器自己的 `extract`（评论/动态选择器 + `data-mid` + 用户主页链接三重兜底），身份 key 与黑名单完全一致，不会出现"拉了黑却没隐藏"的孤儿 key。

### 一键拉黑全部（批量）

- **B站视频页左下角浮层**「🚫 拉黑本页评论用户(N)」：只统计当前已加载的评论作者，不会把右侧推荐视频卡误算为用户。原生举报、登录等弹窗打开时该浮层会隐藏，避免出现无意义的 `(0)`。
- **用户列表弹窗内**：仅当弹窗实际列出至少两条用户空间链接时，弹窗头部才会出现「🚫 拉黑全部(N)」。视频页对普通观众并没有可枚举的“谁赞了”列表时，脚本不会虚构这个入口。
- 适合"某条评论/视频下面一群人全在 output 恶心内容"时快速清场；同样可在设置里关闭。

---

## 设计与风控要点

- **抖音推荐流自动切是唯一的"模拟操作"**，为防雷控设了四道安全阀：① 仅在命中黑名单时触发；② 同一视频只切一次（WeakSet）；③ 200–600ms 随机延迟；④ 连续跳过上限（默认 6 次）超限即停在遮罩不再自动切。设置里可一键降级为纯遮罩。
- **绝不写 `media.muted`** 来静音被跳过视频——抖音把静音当全局偏好，写它会污染你正常刷视频的静音状态（这是 Pynseq 踩过的坑，已规避）。改用视觉遮罩。
- **所有拉黑入口都是自建 UI**，绝不触发平台原生"不感兴趣"或官方拉黑，避免污染推荐模型。
- **B站弹幕**在页面拿到数据前拦截实际的 `seg.so` / `list.so` 弹幕段，手写轻量 protobuf 解析读取 `progress`、`mid_hash`、内容。`bili:uid:N` 经 CRC32 映射，或弹幕列表直接保存 `bili:dmhash:<hash>`；不会重写不含弹幕元素的 `view` 元数据接口。

---

## 致谢 / 许可证

- 整体 **MIT** 许可证。
- **抖音、微博**两个适配器的行为（选择器策略、`data-e2e` 锚点、推荐流跳过纪律、避开原生"不感兴趣"）脱胎自开源项目 **Pynseq-Douyin** 与 **Pynseq-Weibo**（作者 DanielZenFlow，**MIT**）。本项目按其 MIT 许可证保留了原作者版权与许可声明，并在此致谢。
- 其余 4 个适配器（B站/知乎/贴吧/X）及统一身份模型、隐藏引擎、弹幕钩子均为本项目自建。

---

## 验证状态

| 平台 | 当前可验证结果 | 仍需真实登录态复核 |
|---|---|---|
| B站 | 用真实视频页验证了评论组件 `__data.member.mid`、当前 `bili-comment-menu > #options > li` 菜单、评论作者计数和实际可见的批量/本地拉黑 UI；播放器实际用 `XMLHttpRequest(responseType=arraybuffer)` 请求 `seg.so`，该传输路径已回归覆盖。 | 右侧弹幕列表在隔离未登录会话不返回可操作发送者行；`mid_hash` 提取和过滤已由真实段字段 + protobuf 回归测试覆盖，需登录态再确认具体行类名。 |
| 微博 | 用真实公开信息流验证了当前 `article.woo-panel-main` 卡片和 `/u/<uid>` / `usercard` 身份提取。 | 评论区、原生菜单和已登录虚拟列表仍需单独复核。 |
| 知乎 | 作者主键采用 `/people/<token>`；结构回归和成熟脚本资料均支持该模型。 | 隔离浏览器被重定向到登录页，未把它误报为真页通过。 |
| 贴吧 | `data-field` JSON 的 `author.user_id` 结构回归已覆盖。 | 隔离浏览器触发百度安全验证，需正常会话再抓当前帖子/楼中楼结构。 |
| X | `article[data-testid="tweet"]` 与 `/handle` 身份结构回归已覆盖。 | 未登录状态只有登录页，未验证真实时间线和菜单。 |
| 抖音 | `data-e2e="comment-item"` 与 `/user/<sec_uid>` 结构回归已覆盖，参考 Pynseq-Douyin 的现行策略。 | 隔离浏览器落在验证码中间页，评论、弹幕和推荐流必须在正常会话复核。 |

可重复运行的检查：

```powershell
node test/run.cjs                 # 基础 Shadow DOM / 设置回归
node test/quickblock.cjs          # B站 34 卡误计数、菜单、举报框、mid_hash / XHR 8 项回归
node test/adapters.cjs            # 微博、知乎、贴吧、X、抖音身份结构回归
node test/real-bilibili-probe.cjs # 隔离真实 B站页只读探针
node test/real-platform-probe.cjs # 其余平台隔离真实页只读探针
```

参考实现：微博和抖音的策略持续对照 [Pynseq-Weibo](https://github.com/DanielZenFlow/Pynseq-Weibo) 与 [Pynseq-Douyin](https://github.com/DanielZenFlow/Pynseq-Douyin)；知乎的本地屏蔽行为对照 [XIU2/UserScript](https://github.com/XIU2/UserScript/wiki/%E7%9F%A5%E4%B9%8E-%E5%A2%9E%E5%BC%BA)。

---

## 维护与交接

项目维护者和自动化代理必须遵守 [AGENTS.md](AGENTS.md) 的变更、验证、文档与发布门禁；当前验证证据、已知限制和下一步交接事项记录在 [MAINTENANCE.md](MAINTENANCE.md)。
